version: 1
project: demoWormRepo

# Agents installed in this project (managed by agentbox add/install)
agents: {}

# Default agent when running bare 'agentbox'
# default: code-guardian

# Shortcuts: agentbox cg = agentbox code-guardian
shortcuts:
  cg: code-guardian
  pp: perf-profiler
  s: scan
  w: watch

# Shared knowledge injected into all agents
# shared_knowledge:
#   - docs/coding-standards.md
