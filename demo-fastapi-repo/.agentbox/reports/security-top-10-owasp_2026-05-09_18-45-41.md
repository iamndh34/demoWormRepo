# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T11:45:58Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQLi, RCE, XSS, SSRF, IDOR và lộ secrets**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|---------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 23-25 | Query ghép f-string với `username` trực tiếp: attacker chỉ cần `username=' OR '1'='1` là có thể biến câu lệnh thành truy vấn tùy ý, đọc toàn bộ bảng `users`. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 31-32 | `subprocess.check_output(..., shell=True)` nhận `host` từ user. Payload như `8.8.8.8; cat /etc/passwd` có thể chèn lệnh và dẫn tới RCE. [A03] | `subprocess.check_output(["ping", "-c", "1", host], shell=False)` |
| 3 | HIGH | XSS | dòng 38-39 | HTML được render bằng string interpolation với `name` không escape; payload `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | Dùng template auto-escape hoặc `html.escape(name)` trước khi render |
| 4 | HIGH | Hardcoded Secret | dòng 44-45 | `SECRET_KEY` và `API_TOKEN` bị hardcode trong source; nếu repo lộ, attacker có thể dùng khóa này để giả mạo phiên hoặc truy cập tài nguyên liên quan. [A02] | Đọc từ biến môi trường: `SECRET_KEY = os.getenv("SECRET_KEY")` |
| 5 | HIGH | Weak Hash | dòng 47-48 | `hashlib.md5()` cho mật khẩu là yếu và dễ bị brute-force/rainbow table; password yếu như `123456` có thể bị crack rất nhanh. [A02] | `import bcrypt` rồi dùng `bcrypt.hashpw(password.encode(), bcrypt.gensalt())` |
| 6 | HIGH | IDOR | dòng 54-57 | Endpoint lấy `account_id` thẳng từ path và query theo ID mà không kiểm tra owner/permission. Attacker đổi `/account/1` thành `/account/2` là xem được dữ liệu người khác. [A01] | Kiểm tra chủ sở hữu trước khi trả dữ liệu: `WHERE id=? AND owner_id=?` |
| 7 | CRITICAL | Insecure Deserialization | dòng 63-66 | `pickle.loads(body)` trên dữ liệu user-controlled có thể bị lợi dụng để thực thi logic độc hại hoặc RCE; payload pickle độc có thể kích hoạt object gadget khi deserialize. [A08] | Chỉ dùng JSON: `obj = await request.json()` |
| 8 | HIGH | Unsafe YAML Load | dòng 73-75 | `yaml.load(..., Loader=yaml.Loader)` cho phép deserialize object không an toàn; payload YAML có thể tạo object ngoài ý muốn và dẫn tới logic corruption/RCE. [A05] | `config = yaml.safe_load(body)` |
| 9 | HIGH | SSRF | dòng 81-83 | `requests.get(url)` lấy URL từ user mà không allowlist; attacker có thể gọi `http://169.254.169.254/latest/meta-data/` hoặc endpoint nội bộ để đọc metadata/tài nguyên private. [A10] | Chỉ cho phép domain allowlist và chặn IP nội bộ trước khi fetch |
| 10 | HIGH | JWT Bypass | dòng 89-90 | `jwt.decode(..., verify_signature=False)` bỏ kiểm tra chữ ký nên token giả mạo vẫn được chấp nhận; attacker có thể đổi `username` trong payload và tự nhận quyền người khác. [A07] | `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý chứa lỗi, nhưng nếu xuất hiện trong code thật thì project đang exposed nghiêm trọng trên nhiều mặt: injection, auth bypass, SSRF và lộ secrets. Theo mình nên chặn release cho đến khi các điểm CRITICAL/HIGH được xử lý xong.
