# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T05:21:57Z

**⚠ 10 lỗi trên 1 file — project đang exposed nghiêm trọng tới SQL injection, RCE, XSS, SSRF, IDOR, insecure deserialization và lộ secrets**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 23-26 | Query ghép f-string với `username` đi thẳng vào SQL: attacker chỉ cần `username=' OR '1'='1` là có thể dump toàn bộ bảng `users`. [A03] | Dùng query tham số: `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 32-33 | `subprocess.check_output(..., shell=True)` với `host` từ user cho phép chèn `; cat /etc/passwd` hoặc `&& curl ...` để RCE. [A03] | Bỏ `shell=True`, truyền args list: `subprocess.check_output(["ping","-c","1",host], shell=False)` |
| 3 | HIGH | XSS | dòng 39-40 | `name` được nhúng thẳng vào HTML, nên payload như `<img src=x onerror=alert(1)>` sẽ chạy JavaScript trên trình duyệt nạn nhân. [A03] | Escape output: `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 4 | HIGH | Hardcoded Secret | dòng 45-46 | `SECRET_KEY` và `API_TOKEN` là secret nhúng cứng trong source; nếu repo bị lộ, attacker có thể dùng chúng để giả mạo hoặc truy cập dịch vụ liên quan. [A02] | Đưa vào env/secret manager: `SECRET_KEY = os.getenv("SECRET_KEY")` |
| 5 | HIGH | IDOR | dòng 54-57 | `/account/{account_id}` trả dữ liệu theo ID do user chọn mà không kiểm tra quyền sở hữu; đổi `account_id=2` là xem được tài khoản người khác. [A01] | Check ownership trước khi query: `if not can_access(current_user, account_id): raise HTTPException(403)` |
| 6 | CRITICAL | Insecure Deserialization | dòng 64-67 | `pickle.loads(body)` trên dữ liệu do client gửi là cực nguy hiểm: attacker có thể gửi payload pickle độc hại để thực thi code khi deserialize. [A08] | Đổi sang JSON an toàn: `obj = json.loads(body)` |
| 7 | HIGH | Unsafe YAML Load | dòng 74-76 | `yaml.load(..., Loader=yaml.Loader)` có thể khởi tạo object tùy ý; payload YAML crafted có thể dẫn tới thực thi hành vi nguy hiểm. [A05] | Dùng `yaml.safe_load(body)` |
| 8 | HIGH | SSRF | dòng 82-84 | `requests.get(url)` với URL từ user cho phép gọi tới internal metadata/service như `http://169.254.169.254/` hoặc `http://localhost:...`. [A10] | Allowlist host/scheme: `assert urlparse(url).hostname in ALLOWED_HOSTS` |
| 9 | HIGH | JWT Bypass | dòng 90-91 | `jwt.decode(..., verify_signature=False)` bỏ verify chữ ký nên attacker có thể sửa token và tự nhận là bất kỳ user nào. [A07] | Verify chữ ký bắt buộc: `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` |
| 10 | HIGH | Secret Logging | dòng 97-102 | Log thẳng `password` vào file/console khiến credential bị lộ qua log aggregation; chỉ cần đọc log là thấy mật khẩu thật. [A09] | Không log secret: `logging.info("Login attempt: user=%s", username)` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý cài lỗi để test, nhưng nếu xuất hiện trong code thật thì project đang exposed rất nặng và mình khuyên chặn release ngay. Đặc biệt nhóm CRITICAL (SQL injection, command injection, insecure deserialization) có thể dẫn tới đọc dữ liệu hoặc RCE trực tiếp.
