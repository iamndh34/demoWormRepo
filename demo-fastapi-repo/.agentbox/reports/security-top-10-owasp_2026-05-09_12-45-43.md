# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T05:45:59Z

**⚠ 10 lỗi trên 1 file — project đang exposed nghiêm trọng tới RCE, SQLi, SSRF và auth bypass**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 25-29 | Query ghép `username` bằng f-string rồi đưa thẳng vào `cursor.execute()`; attacker chỉ cần nhập `' OR '1'='1` là có thể đọc toàn bộ bảng users. [A03] | Dùng query tham số: `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 37-38 | `subprocess.check_output(..., shell=True)` với `host` từ user làm payload như `8.8.8.8; cat /etc/passwd` chạy được lệnh tùy ý. Đây là exposure tới RCE. [A03] | Bỏ `shell=True`, dùng args list: `subprocess.check_output(["ping","-c","1", host], shell=False)` |
| 3 | HIGH | XSS | dòng 44-46 | HTML trả về ghép thẳng `name` vào response; payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | Escape output hoặc render bằng template auto-escape: `return HTMLResponse(html.escape(name))` |
| 4 | HIGH | Cryptographic Failures | dòng 51-55 | `SECRET_KEY`/`API_TOKEN` hardcoded trong source và `hashlib.md5()` dùng để hash password. Attacker có thể trích secret từ repo và bẻ mật khẩu bằng rainbow table. [A02] | Đưa secret sang env/secret manager và dùng `passlib` bcrypt: `pwd_context.hash(password)` |
| 5 | HIGH | IDOR | dòng 61-65 | `/account/{account_id}` đọc record theo `account_id` mà không có auth/ownership check; đổi `account_id=1` là xem được dữ liệu người khác. [A01] | Thêm `current_user=Depends(...)` rồi kiểm tra quyền trước khi query |
| 6 | CRITICAL | Insecure Deserialization | dòng 72-75 | `pickle.loads(body)` trên input từ request cho phép payload pickle độc hại thực thi code khi deserialize. Ví dụ payload có `__reduce__` có thể dẫn tới RCE. [A08] | Không dùng `pickle` cho data không tin cậy; chuyển sang JSON/schema validation |
| 7 | HIGH | Unsafe YAML Load | dòng 81-84 | `yaml.load(..., Loader=yaml.Loader)` có thể instantiate object tùy ý với payload YAML độc hại. Kết hợp `DEBUG = True` làm lộ thêm thông tin khi lỗi xảy ra. [A05] | Dùng `yaml.safe_load(body)` và tắt debug: `DEBUG = False` |
| 8 | CRITICAL | SSRF | dòng 91-93 | `requests.get(url)` fetch URL do user kiểm soát; attacker có thể trỏ vào `http://169.254.169.254/` để đọc metadata hoặc chọc vào internal services. [A10] | Allowlist host và chặn IP nội bộ trước khi gọi `requests.get()` |
| 9 | CRITICAL | JWT Bypass | dòng 99-101 | `jwt.decode(..., verify_signature=False)` bỏ qua xác thực chữ ký; attacker tự ký token giả là có thể mạo danh bất kỳ user nào. [A07] | Bật verify chữ ký và kiểm tra `algorithms=["HS256"]`: `jwt.decode(token, key=JWT_SECRET, algorithms=["HS256"])` |
| 10 | MEDIUM | Sensitive Data Logging | dòng 107-110 | Log trực tiếp `password` làm lộ credential vào log/observability pipeline; ai đọc log là có thể đăng nhập lại. [A09] | Chỉ log metadata: `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý chứa lỗi để test. Nếu là code thật thì project đang exposed rất nặng và nên chặn release ngay, vì đã có đường tới SQLi, RCE, SSRF và auth bypass.
