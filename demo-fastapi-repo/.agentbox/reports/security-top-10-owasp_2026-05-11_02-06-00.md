# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-10T19:06:16Z

**⚠ 10 lỗi trên 1 file — project đang exposed nghiêm trọng tới nhiều lỗ hổng OWASP Top 10**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Hardcoded Secret | dòng 44-45 | `SECRET_KEY` và `API_TOKEN` được nhúng thẳng trong mã nguồn; nếu đây là secret thật, attacker lấy được khóa ký JWT/cụm AWS token và có thể chiếm quyền hoặc truy cập tài nguyên cloud. Ví dụ: rò rỉ repo là đủ để dùng `AKIA...` tấn công tiếp. [A02] |
| 2 | CRITICAL | SQL Injection | dòng 24-27 | Query dùng f-string chèn trực tiếp `username` vào SQL rồi `execute(query)`; attacker có thể nhập `username = `' OR '1'='1' --` để dump toàn bộ bảng users. [A03] |
| 3 | CRITICAL | Command Injection | dòng 33-34 | `subprocess.check_output(..., shell=True)` nhận `host` từ user; payload kiểu `127.0.0.1; cat /etc/passwd` có thể biến request thành RCE/đọc file hệ thống. [A03] |
| 4 | HIGH | XSS | dòng 40-41 | Response HTML ghép thẳng `name` vào body mà không escape; payload `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. Đây là exposure tới reflected XSS. [A03] |
| 5 | HIGH | IDOR | dòng 51-54 | Endpoint `account_id` lấy trực tiếp từ path và truy vấn bản ghi theo id mà không thấy kiểm tra chủ sở hữu/quyền truy cập; attacker chỉ cần đổi `account_id=2` để xem tài khoản người khác. [A01] |
| 6 | CRITICAL | Deserialization | dòng 60-63 | `pickle.loads(body)` trên dữ liệu request là unsafe deserialization; payload pickle độc hại có thể kích hoạt gadget chain và dẫn tới RCE. Ví dụ: gửi blob pickle crafted thay vì JSON. [A08] |
| 7 | HIGH | Unsafe YAML | dòng 69-72 | `yaml.load(..., Loader=yaml.Loader)` xử lý body không tin cậy; YAML tags có thể instantiate object nguy hiểm hoặc mở đường tới code execution tùy gadget. Ví dụ: payload `!!python/object/apply:os.system ["id"]`. [A05] |
| 8 | HIGH | SSRF | dòng 78-80 | `requests.get(url)` fetch thẳng URL do user chọn, không có allowlist/kiểm tra host; attacker có thể trỏ tới `http://localhost:...` hoặc metadata endpoint để đọc tài nguyên nội bộ. [A10] |
| 9 | CRITICAL | JWT Bypass | dòng 86-87 | `jwt.decode(..., verify_signature=False)` bỏ kiểm tra chữ ký; attacker tự tạo token giả với `username=admin` để giả mạo danh tính. Đây là auth bypass rõ ràng. [A07] |
| 10 | HIGH | Secret Logging | dòng 92-96 | `logging.info` ghi cả `password` vào log; ai đọc được log sẽ lấy được credential thô. Ví dụ: user đăng nhập thử là mật khẩu lộ ngay trong file log. [A09] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý chứa lỗi để test, nên các finding là expected. Nếu đây là code thật thì project đang exposed nghiêm trọng ở nhiều điểm OWASP Top 10 và nên chặn release ngay.
