# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-14T07:11:00Z

**⚠ 10 lỗi trên 4 file — project đang EXPOSED tới SQL injection, JWT bypass, SSRF, XSS và secret leak**

### `vulnerable_demo.py` (6 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 28-29 | `subprocess.check_output(..., shell=True)` ghép thẳng `host` từ user vào shell. Attacker có thể chèn `; cat /etc/passwd` hoặc `&& curl ...` để chạy lệnh tùy ý trên server. [A03] |
| 2 | CRITICAL | SQL Injection | dòng 20-22 | Query SQL dùng f-string với `username` không escape. Ví dụ `username = `' OR '1'='1` sẽ làm query trả về toàn bộ user. [A03] |
| 3 | CRITICAL | JWT Bypass | dòng 107 | `jwt.decode(..., options={"verify_signature": False})` bỏ qua xác thực chữ ký. Attacker có thể tự sửa payload token, ví dụ đổi `username` thành `admin`, rồi gửi token giả mạo. [A07] |
| 4 | HIGH | Insecure Deserialization | dòng 77 | `pickle.loads(body)` trên dữ liệu từ request là đường vào RCE/logic abuse nếu attacker gửi payload pickle độc hại. Chỉ cần body được craft đúng là có thể kích hoạt object gadget. [A08] |
| 5 | HIGH | SSRF | dòng 98-99 | `requests.get(url)` lấy URL trực tiếp từ user. Attacker có thể ép server gọi nội bộ như `http://169.254.169.254/latest/meta-data/` để đọc metadata hoặc quét mạng nội bộ. [A10] |
| 6 | HIGH | Secret Exposure | dòng 54 | `API_TOKEN = "AKIAIOSFODNN7EXAMPLE"` là secret/credential hardcoded trong source. Nếu đây là token thật, attacker có thể dùng ngay; nếu là placeholder thì vẫn là pattern nguy hiểm cần kiểm tra kỹ. [A02] |

### `auth.py` (3 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 20-22 | Query login ghép `username` và `password` trực tiếp vào SQL. Payload kiểu `admin' --` hoặc `' OR 1=1 --` có thể bypass đăng nhập và đọc dữ liệu user. [A03] |
| 2 | HIGH | JWT Bypass | dòng 42 | `jwt.decode(token, options={"verify_signature": False})` cho phép token giả mạo đi qua như hợp lệ. Attacker chỉ cần sửa payload rồi base64-encode lại là có thể mạo danh. [A07] |
| 3 | MEDIUM | Timing Attack | dòng 34-37 | So sánh token bằng `==` có thể lộ khác biệt thời gian khi đo lặp lại, giúp attacker suy đoán giá trị đúng từng phần trong một số bối cảnh. Đây không phải RCE, nhưng vẫn là exposure đáng chú ý trên đường xác thực. [A02] |

### `main.py` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | MEDIUM | SQL Injection | dòng 36 | `cursor.execute(f"SELECT * FROM profiles WHERE user_id = {u_id}")` vẫn là query ghép chuỗi. Nếu `u_id` bị ảnh hưởng bởi dữ liệu không tin cậy ở upstream, attacker có thể chèn điều kiện SQL để đọc sai dữ liệu. [A03] |

### `migrations/001_risky_change.sql` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | MEDIUM | Destructive Migration | dòng 2 | `ALTER TABLE users DROP COLUMN email;` là thay đổi phá hủy dữ liệu. Nếu chạy nhầm trên production mà không có backup/rollback plan, có thể gây mất dữ liệu và gián đoạn dịch vụ. |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py, auth.py, main.py, migrations/001_risky_change.sql |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Code này là demo cố ý chứa lỗi, nhưng nếu đây là code thật thì project đang exposed rất nặng ở các điểm auth, injection và secret handling. Với mức CRITICAL xuất hiện ở đường vào public-facing, mình khuyên chặn release ngay cho tới khi các điểm này được xử lý.
