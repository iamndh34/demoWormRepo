# code-guardian — saved by agentbox --save

Generated: 2026-05-08T10:22:22Z

### Báo cáo lỗ hổng bảo mật `vulnerable_demo.py`

| #  | Loại lỗ hổng (OWASP Top 10)                                  | Vị trí (Dòng)       | Chi tiết mô tả                                                                                           | Minh họa code                |
|----|--------------------------------------------------------------|---------------------|----------------------------------------------------------------------------------------------------------|------------------------------|
| 1  | **A03:2021 – SQL Injection**                                 | 28                  | Truy vấn SQL được ghép chuỗi trực tiếp với user input, không dùng tham số                                | `cursor.execute(query)`      |
| 2  | **A03:2021 – OS Command Injection**                          | 38                  | Gọi lệnh hệ điều hành với input không kiểm soát (host), có thể bị inject câu lệnh nguy hiểm              | `subprocess.check_output()`  |
| 3  | **A03:2021 – Cross-Site Scripting (XSS)**                    | 47                  | Render HTML với dữ liệu không được lọc (name), dẫn đến XSS                                               | `f"<h1>Hello, {name}!"...`   |
| 4  | **A02:2021 – Cryptographic Failures (Weak Hash, Secrets)**   | 53, 54, 57          | Dùng MD5 (yếu) cho mật khẩu và hardcode secret/to token trong code                                       | MD5, SECRET_KEY, API_TOKEN   |
| 5  | **A01:2021 – Broken Access Control (IDOR)**                  | 67                  | Không xác thực quyền truy cập tài khoản theo `account_id`, ai cũng có thể truy vấn mọi dữ liệu            | `cursor.execute(f"SELECT...` |
| 6  | **A08:2021 – Insecure Deserialization**                      | 77                  | Dùng `pickle.loads` trực tiếp với user input, có thể bị thực thi mã nguy hiểm                            | `pickle.loads(body)`         |
| 7  | **A05:2021 – Security Misconfiguration (YAML/Debug mode)**   | 84, 89              | Bật chế độ DEBUG, `yaml.load` với user input mà không dùng safe loader                                   | `yaml.load(body, Loader=...)`|
| 8  | **A10:2021 – SSRF (Server-Side Request Forgery)**            | 98                  | Người dùng cung cấp URL, server thực hiện request ra ngoài, tiềm ẩn SSRF                                 | `requests.get(url)`          |
| 9  | **A07:2021 – JWT None Algorithm (Auth Bypass)**              | 107                 | Cho phép JWT decode không kiểm tra chữ ký, dùng options nguy hiểm                                        | `jwt.decode(...verify_signature: False)`|
| 10 | **A09:2021 – Logging Secrets**                               | 118                 | Ghi lại thông tin đăng nhập (username, password) vào log, rò rỉ thông tin nhạy cảm                       | `logging.info(...)`          |

---

**Kiến nghị:** Không deploy code này vào môi trường thật; từng điểm trên đều là mô tả lỗ hổng nghiêm trọng!  
Bạn cần hướng dẫn fix chi tiết từng lỗi? Hãy hỏi!
