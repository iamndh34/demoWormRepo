# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T11:39:45Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQL injection, RCE, XSS, SSRF và rò rỉ secrets**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | Command Injection | dòng 29 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` hoặc `&& id` để thực thi lệnh tùy ý. [A03] | `subprocess.check_output(["ping","-c","1",host], shell=False)` |
| 2 | CRITICAL | Deserialization | dòng 56-58 | `pickle.loads(body)` có thể bị lợi dụng để chạy code khi nhận payload độc hại; chỉ cần gửi pickle crafted là attacker kiểm soát luồng thực thi. [A08] | Dùng JSON: `obj = json.loads(body)` hoặc whitelist schema |
| 3 | CRITICAL | SSRF | dòng 77-78 | `requests.get(url)` lấy URL trực tiếp từ user nên attacker có thể gọi metadata/internal service như `http://169.254.169.254/latest/meta-data/` để đọc tài nguyên nội bộ. [A10] | Parse URL và chặn host/private IP trước khi `requests.get()` |
| 4 | HIGH | SQL Injection | dòng 22-25 | Query ghép f-string với `username` nên payload như `x' OR '1'='1` sẽ làm lệch điều kiện và dump bảng `users`. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 5 | HIGH | XSS | dòng 35-36 | Trả HTML trực tiếp với `name` chưa escape; payload `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | `from html import escape; return f"<html>...{escape(name)}..."` |
| 6 | HIGH | IDOR | dòng 50-52 | `account_id` từ path được dùng thẳng để truy vấn `accounts`; attacker đổi `/account/1` sang `/account/2` để xem dữ liệu người khác. [A01] | Kiểm tra quyền sở hữu trước khi query: `if account.owner_id != current_user.id: raise HTTPException(403)` |
| 7 | HIGH | JWT bypass | dòng 84-85 | `jwt.decode(..., options={"verify_signature": False})` tắt verify chữ ký nên token giả mạo cũng được chấp nhận; attacker đổi payload username thành admin. [A07] | `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` |
| 8 | HIGH | Secret Exposure | dòng 41-42 | `SECRET_KEY` và `API_TOKEN` hardcoded trong source; nếu repo lộ thì attacker reuse key/token để truy cập dịch vụ khác. | Đưa vào env/secret manager: `SECRET_KEY=os.environ["SECRET_KEY"]` |
| 9 | MEDIUM | Weak Hash | dòng 44-45 | `hashlib.md5()` rất yếu cho mật khẩu; attacker có thể rainbow-table hoặc brute-force nhanh để phục hồi password. [A02] | `import bcrypt; bcrypt.hashpw(password.encode(), bcrypt.gensalt())` |
| 10 | MEDIUM | Logging secret | dòng 92-93 | `logging.info(...)` ghi thẳng `password` vào log; chỉ cần đọc log là lộ credential của người dùng. [A09] | `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý chứa lỗi, nhưng nếu xuất hiện trong code thật thì project đang exposed rất nghiêm trọng và nên chặn release ngay. Ưu tiên xử lý các lỗi CRITICAL/HIGH trước vì chúng có thể dẫn tới RCE, đọc dữ liệu nội bộ và chiếm quyền truy cập.
