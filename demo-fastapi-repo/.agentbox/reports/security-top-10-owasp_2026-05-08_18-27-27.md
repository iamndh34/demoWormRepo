# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-08T11:27:40Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQL injection, RCE, XSS, SSRF và lộ secret**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | Command Injection | dòng 31 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` để RCE. Đây là đường tấn công trực tiếp vào OS command layer. [A03] | `subprocess.run(["ping","-c","1", host], check=True, capture_output=True)` |
| 2 | HIGH | SQL Injection | dòng 24-26 | Query ghép f-string với `username`; payload như `' OR '1'='1` có thể dump toàn bộ bảng `users`. Attacker chỉ cần điều khiển tham số `username`. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 3 | HIGH | SSRF | dòng 83 | `requests.get(url)` lấy URL từ user; attacker có thể trỏ vào `http://169.254.169.254/latest/meta-data/` để đọc metadata nội bộ. [A10] | `if not url.startswith("https://example.com/"): raise HTTPException(400)` |
| 4 | HIGH | Hardcoded Secret | dòng 45-46 | `SECRET_KEY` và `API_TOKEN` hardcode trong source làm lộ credential nếu repo bị đọc hoặc leak. Giá trị thật có thể bị dùng để giả mạo session / truy cập dịch vụ. [A02] | `SECRET_KEY = os.getenv("SECRET_KEY")` |
| 5 | HIGH | Insecure Deserialization | dòng 64-66 | `pickle.loads(body)` có thể thực thi code tùy ý nếu attacker gửi pickle payload độc hại. Ví dụ payload có thể kích hoạt object gadget chain. [A08] | `obj = await request.json()` |
| 6 | HIGH | JWT Bypass | dòng 89-90 | `jwt.decode(..., verify_signature=False)` bỏ kiểm tra chữ ký, nên token giả mạo vẫn được chấp nhận. Attacker chỉ cần đổi `username` trong payload là qua mặt auth. [A07] | `payload = jwt.decode(token, os.getenv("JWT_SECRET"), algorithms=["HS256"])` |
| 7 | MEDIUM | Weak Hash | dòng 49-50 | `hashlib.md5(password.encode()).hexdigest()` dùng MD5, dễ bị rainbow table / collision; mật khẩu yếu là bị crack nhanh. [A02] | `hashlib.pbkdf2_hmac("sha256", password.encode(), salt, 200000)` |
| 8 | MEDIUM | XSS | dòng 39 | Trả HTML với `{name}` không escape; payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt. Đây là phản xạ XSS rõ ràng. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 9 | MEDIUM | IDOR | dòng 57-59 | `account_id` lấy trực tiếp từ path và query account theo ID, không thấy kiểm tra quyền sở hữu. Attacker đoán `account_id=2` có thể xem tài khoản người khác. [A01] | `if account.owner_id != current_user.id: raise HTTPException(403)` |
| 10 | LOW | Secret Logging | dòng 98-100 | `logging.info(... password={password})` ghi plaintext password vào log. Nếu log bị xem trộm, credential bị lộ ngay. [A09] | `logging.info("Login attempt: user=%s", username)` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |
| Kết luận | Đây là file demo có lỗi cố ý, nhưng mức exposure vẫn rất cao; nếu là code thật thì nên block release ngay. |
