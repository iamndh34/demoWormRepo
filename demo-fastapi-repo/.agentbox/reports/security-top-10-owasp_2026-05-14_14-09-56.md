# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-14T07:10:23Z

**⚠ 10 lỗi trên 4 file — project đang exposed tới SQL injection, RCE, XSS, SSRF và leak secret**

### `vulnerable_demo.py` (8 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 31-32 | `subprocess.check_output(..., shell=True)` ghép thẳng `host` từ user vào shell. Attacker có thể chèn `8.8.8.8; cat /etc/passwd` để chạy lệnh tùy ý trên server. [A03] |
| 2 | CRITICAL | Secret Exposure | dòng 43-44 | Hardcoded `SECRET_KEY` và `API_TOKEN` nằm trực tiếp trong source. Nếu repo bị lộ, attacker có thể dùng key thật để giả mạo phiên hoặc truy cập dịch vụ liên quan. [A02] |
| 3 | CRITICAL | Insecure Deserialization | dòng 61-64 | `pickle.loads(body)` nhận dữ liệu từ request body mà không kiểm soát. Payload pickle độc hại có thể kích hoạt code execution khi server giải tuần tự hóa. [A08] |
| 4 | CRITICAL | JWT Bypass | dòng 79-81 | `jwt.decode(..., verify_signature=False)` bỏ qua xác minh chữ ký hoàn toàn. Attacker chỉ cần sửa payload token, ví dụ đổi `username` thành `admin`, là có thể giả mạo danh tính. [A07] |
| 5 | HIGH | SQL Injection | dòng 22-25 | Query dùng f-string với `username` trực tiếp trong `WHERE`. Payload như `' OR '1'='1` có thể làm lộ toàn bộ bảng `users`. [A03] |
| 6 | HIGH | XSS | dòng 38-39 | HTML response nhúng thẳng `name` vào body mà không escape. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] |
| 7 | HIGH | IDOR | dòng 51-54 | `account_id` từ URL được dùng thẳng để đọc bản ghi `accounts` mà không thấy kiểm tra quyền sở hữu. Attacker chỉ cần đổi ID, ví dụ `/account/2`, để xem dữ liệu tài khoản khác. [A01] |
| 8 | HIGH | SSRF | dòng 71-73 | `requests.get(url)` fetch trực tiếp URL do user cung cấp. Attacker có thể trỏ tới `http://169.254.169.254/latest/meta-data/` để đọc metadata nội bộ hoặc quét mạng private. [A10] |

### `main.py` (2 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | SQL Injection | dòng 24-25 | `u_id` được nhét vào f-string trong query `profiles`. Nếu giá trị đầu vào bị thao túng ở upstream, attacker có thể sửa điều kiện SQL và đọc dữ liệu ngoài ý định. [A03] |
| 2 | MEDIUM | Blocking IO | dòng 31-32 | `requests.get()` chạy trong `async def` sẽ chặn event loop. Đây không phải lỗ hổng bảo mật trực tiếp, nhưng có thể bị lợi dụng để làm chậm dịch vụ khi endpoint bị gọi dồn dập. |

### `migrations/001_risky_change.sql` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | MEDIUM | Destructive Migration | dòng 2 | `ALTER TABLE users DROP COLUMN email;` xóa dữ liệu cột mà không thấy bước backup/rollback. Nếu chạy nhầm trên production, có thể gây mất dữ liệu và gián đoạn khôi phục. |

### `templates/index.html` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | LOW | Accessibility Issue | dòng 22 | Nút có `aria-label` và handler `onclick`, nhưng comment cho thấy nhãn/mục đích có thể gây hiểu nhầm cho người dùng. Đây là vấn đề chất lượng giao diện hơn là lỗ hổng OWASP trực tiếp. |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py, main.py, migrations/001_risky_change.sql, templates/index.html |
| Tổng finding | 12 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Mã này có vẻ là demo cố ý chứa lỗi, nhưng nếu phần tương tự đang nằm trong code thật thì project đang exposed rất nặng: có đường vào RCE, bypass auth, leak secret và SSRF. Theo mình nên chặn release ngay cho đến khi các điểm CRITICAL/HIGH được xử lý xong.
