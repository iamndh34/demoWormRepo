# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T06:00:59Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQLi, RCE, SSRF và bypass xác thực**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 24-29 | Query ghép f-string với `username` rồi đưa thẳng vào `cursor.execute(query)`. Attacker chỉ cần nhập `username = `' OR '1'='1` để dump toàn bộ bảng users. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép chèn `; cat /etc/passwd` để chạy lệnh tuỳ ý trên server. [A03] | `subprocess.check_output(["ping", "-c", "1", host], shell=False)` |
| 3 | HIGH | XSS | dòng 45-47 | HTML trả về chứa trực tiếp `name` trong `<h1>`. Payload như `<img src=x onerror=alert(1)>` sẽ chạy trong trình duyệt nạn nhân. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 4 | HIGH | Cryptographic Failures | dòng 52-58 | `SECRET_KEY`/`API_TOKEN` hardcoded trong source và `hashlib.md5` dùng để hash mật khẩu. MD5 bị crack rất nhanh; secret lộ là attacker có thể giả mạo token. [A02] | `pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")` |
| 5 | HIGH | IDOR | dòng 62-68 | `/account/{account_id}` không có kiểm tra quyền, chỉ dựa vào `account_id`. Attacker đổi ID từ `1` sang `2` là đọc được tài khoản người khác. [A01] | `if not can_view_account(current_user, account_id): raise HTTPException(403)` |
| 6 | CRITICAL | Insecure Deserialization | dòng 73-77 | `pickle.loads(body)` trên data từ request là đường thẳng tới RCE; payload pickle độc hại có thể chạy code khi deserialize. [A08] | `obj = json.loads(body)` hoặc chỉ deserialize định dạng an toàn, có schema |
| 7 | HIGH | Unsafe YAML | dòng 82-87 | `yaml.load(..., Loader=yaml.Loader)` có thể kích hoạt object construction nguy hiểm. Payload YAML crafted có thể dẫn tới execution/hành vi ngoài ý muốn. [A05] | `config = yaml.safe_load(body)` |
| 8 | HIGH | SSRF | dòng 92-95 | `requests.get(url)` lấy URL từ user mà không allowlist. Attacker có thể trỏ tới `http://169.254.169.254/latest/meta-data/` để đọc metadata nội bộ. [A10] | `if urlparse(url).hostname not in ALLOWED_HOSTS: raise ValueError()` |
| 9 | CRITICAL | JWT Bypass | dòng 100-103 | `jwt.decode(..., verify_signature=False)` bỏ kiểm tra chữ ký hoàn toàn. Chỉ cần tự ký/đổi payload là có thể giả mạo user bất kỳ. [A07] | `jwt.decode(token, key=JWT_SECRET, algorithms=["HS256"], options={"verify_signature": True})` |
| 10 | HIGH | Secret Logging | dòng 109-113 | Log thẳng `password` ra file/console làm lộ credential qua log aggregation. Payload hợp lệ vẫn bị ghi lại nguyên văn, rất dễ bị exfiltrate nội bộ. [A09] | `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | `vulnerable_demo.py` |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Mã này rõ ràng là demo cố ý chứa lỗi để test scanner. Nếu đây là code thật thì project đang exposed nghiêm trọng ở nhiều điểm CRITICAL và nên chặn release ngay cho tới khi fix xong các sink chính.
