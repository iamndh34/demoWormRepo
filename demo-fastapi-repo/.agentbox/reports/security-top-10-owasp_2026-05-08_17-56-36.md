# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-08T10:56:50Z

**⚠ 10 lỗi trên 1 file — project đang exposed tới SQL injection, RCE, XSS, SSRF và lộ secrets**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 26-29 | Query ghép chuỗi với `username` đi thẳng vào SQL: attacker chỉ cần `username = '\' OR '1'='1` là đọc được toàn bộ bảng users. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | HIGH | Command Injection | dòng 38 | `shell=True` + f-string với `host` cho phép chèn `; cat /etc/passwd` hoặc `&& id` để RCE. [A03] | `subprocess.check_output(["ping", "-c", "1", host], shell=False)` |
| 3 | HIGH | XSS | dòng 47 | Trả HTML bằng f-string với `name` chưa escape, nên payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 4 | HIGH | Hardcoded Secret | dòng 52-53 | `SECRET_KEY` và `API_TOKEN` hardcode trong source; nếu repo bị lộ, attacker có thể giả mạo session hoặc dùng token thật. [A02] | Đưa secret sang env: `SECRET_KEY = os.getenv("SECRET_KEY")` |
| 5 | HIGH | Weak Hash | dòng 55-56 | MD5 không an toàn cho mật khẩu; attacker dùng rainbow table/bruteforce để bẻ rất nhanh. [A02] | `return bcrypt.hashpw(password.encode(), bcrypt.gensalt()).decode()` |
| 6 | HIGH | IDOR | dòng 65-68 | Truy cập `/account/{account_id}` chỉ dựa vào ID, không kiểm tra quyền sở hữu; đổi `account_id=2` là đọc tài khoản người khác. [A01] | Kiểm tra ownership trước khi trả dữ liệu: `if account.owner_id != current_user.id: raise HTTPException(403)` |
| 7 | HIGH | Insecure Deserialization | dòng 77-79 | `pickle.loads(body)` trên dữ liệu user-controlled có thể dẫn tới RCE; payload pickle độc hại có thể chạy code khi deserialize. [A08] | Chuyển sang JSON: `obj = await request.json()` |
| 8 | HIGH | Unsafe YAML Load | dòng 89 | `yaml.load(..., Loader=yaml.Loader)` cho phép construct object nguy hiểm; attacker có thể khai thác để instantiate payload tuỳ ý. [A05] | `config = yaml.safe_load(body)` |
| 9 | HIGH | SSRF | dòng 98 | `requests.get(url)` lấy URL từ user, attacker có thể ép server gọi `http://169.254.169.254/latest/meta-data/` để lấy metadata/internal service. [A10] | Whitelist domain/scheme trước khi fetch: `if not url.startswith("https://trusted/"): raise HTTPException(400)` |
| 10 | HIGH | Secret Logging | dòng 118 | Log trực tiếp `password` làm lộ credential vào log aggregation; ai đọc log là thấy mật khẩu thật. [A09] | `logging.info("Login attempt: user=%s", username)` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | `vulnerable_demo.py` |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |
| Mẫu tấn công nổi bật | `\' OR \'1\'=\'1`, `; cat /etc/passwd`, `<script>alert(1)</script>`, `http://169.254.169.254/latest/meta-data/` |
| Kết luận | Đây là demo có chủ đích nhưng nếu lọt vào production thì project đang exposed rất nặng; theo mình nên block release cho đến khi tách hết các sink nguy hiểm. |
