# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T05:46:24Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQLi, RCE, SSRF, XSS và auth bypass**  
Xin chào, mình đã quét `vulnerable_demo.py` và đây là report dạng bảng.

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 24-29 | Query ghép trực tiếp `username` vào f-string rồi `execute(query)`. Attacker chỉ cần `username = `' OR '1'='1` để đọc toàn bộ bảng users. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 36-38 | `subprocess.check_output(..., shell=True)` với `host` từ request — payload như `8.8.8.8; cat /etc/passwd` có thể dẫn tới RCE. [A03] | `subprocess.check_output(["ping","-c","1",host], shell=False)` |
| 3 | HIGH | XSS | dòng 45-47 | Trả HTML chứa thẳng `name` từ user, nên payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | `from html import escape; return f"...{escape(name)}..."` |
| 4 | HIGH | Cryptographic Failure | dòng 51-57 | `SECRET_KEY` và `API_TOKEN` hardcoded trong source, đồng thời dùng `md5` để hash mật khẩu. Đây là exposure lớn: secret bị lộ và mật khẩu có thể bị crack rất nhanh. [A02] | `from passlib.context import CryptContext; pwd=CryptContext(schemes=["bcrypt"]); hash=pwd.hash(password)` |
| 5 | HIGH | IDOR | dòng 63-67 | Endpoint `/account/{account_id}` tin trực tiếp path param và trả dữ liệu theo ID mà không kiểm tra quyền sở hữu. Attacker đổi `account_id=1,2,3...` là đọc được tài khoản người khác. [A01] | `if not can_view_account(current_user, account_id): raise HTTPException(403)` |
| 6 | CRITICAL | Insecure Deserialization | dòng 74-78 | `pickle.loads(body)` trên dữ liệu từ request là cực kỳ nguy hiểm; payload pickle độc hại có thể thực thi code khi deserialize. [A08] | `obj = json.loads(body)` |
| 7 | HIGH | Security Misconfiguration | dòng 86-89 | `yaml.load(..., Loader=yaml.Loader)` cho phép construct object nguy hiểm từ YAML; cộng thêm `DEBUG = True` làm lộ thông tin nhạy cảm khi lỗi xảy ra. [A05] | `config = yaml.safe_load(body); DEBUG = False` |
| 8 | CRITICAL | SSRF | dòng 96-99 | `requests.get(url)` fetch theo URL user cung cấp, nên attacker có thể trỏ tới `http://169.254.169.254/...` hoặc internal admin service để đọc dữ liệu nội bộ. [A10] | `if urlparse(url).hostname not in ALLOWED_HOSTS: raise ValueError()` |
| 9 | CRITICAL | JWT Bypass | dòng 105-108 | `jwt.decode(..., verify_signature=False)` bỏ qua xác minh chữ ký, nên attacker có thể tự sửa token và mạo danh bất kỳ user nào. [A07] | `jwt.decode(token, JWT_SECRET, algorithms=["HS256"])` |
| 10 | HIGH | Secret Logging | dòng 116-120 | Log thẳng `password` ra file/console là lộ credential; chỉ cần xem log là thấy mật khẩu thật. [A09] | `logging.info("Login attempt: user=%s", username)` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | `vulnerable_demo.py` |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý chứa lỗi, nhưng nếu xuất hiện trong code thật thì project đang exposed rất nặng và nên chặn release ngay. Mức rủi ro tổng thể là cực cao vì có cả SQL injection, command injection, SSRF, auth bypass và deserialization nguy hiểm.
