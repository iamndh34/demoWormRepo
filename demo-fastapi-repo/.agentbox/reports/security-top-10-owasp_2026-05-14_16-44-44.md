# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-14T09:44:57Z

**⚠ 10 lỗi trên 5 file — project đang exposed tới SQL injection, SSRF, JWT bypass và secret leak**

### `vulnerable_demo.py` (5 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` hoặc `&& curl ...` để RCE. Đây là A03 và có thể dẫn tới chiếm quyền máy chủ ngay. |
| 2 | HIGH | SQL Injection | dòng 29 | Query ghép chuỗi trực tiếp với `username` rồi `cursor.execute(query)`. Payload kiểu `' OR '1'='1` có thể làm lộ toàn bộ bảng `users`. [A03] |
| 3 | HIGH | IDOR | dòng 67 | `account_id` từ URL được dùng thẳng trong query mà không thấy kiểm tra ownership. Attacker chỉ cần đổi `/account/1` thành `/account/2` để đọc dữ liệu người khác. [A01] |
| 4 | HIGH | SSRF | dòng 98 | `requests.get(url)` nhận URL từ user và server sẽ đi fetch hộ. Payload như `http://169.254.169.254/latest/meta-data/` có thể chạm metadata service hoặc internal host. [A10] |
| 5 | CRITICAL | JWT bypass | dòng 107 | `jwt.decode(..., verify_signature=False)` bỏ qua xác thực chữ ký, nên attacker có thể tự sửa payload rồi giả mạo user. Ví dụ đổi `username` thành `admin` là có thể impersonate. [A07] |

### `Dockerfile` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | Secret Leak | dòng 14 | `ARG GITHUB_TOKEN=ghp_secret_123456789` nhúng secret vào build context/history. Nếu image hoặc layer bị lộ, token thật có thể bị trích xuất. [A02] |

### `auth.py` (3 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | SQL Injection | dòng 29 | `query = f"SELECT * FROM users WHERE username = '{username}' AND password = '{password}'"` cho phép payload như `' OR 1=1 --` để bypass login. Đây là A03 và ảnh hưởng trực tiếp tới xác thực. |
| 2 | HIGH | JWT bypass | dòng 42 | `jwt.decode(token, options={"verify_signature": False})` chấp nhận token không kiểm chữ ký. Attacker có thể tự ký giả hoặc sửa payload để mạo danh. [A07] |
| 3 | MEDIUM | Timing Attack | dòng 35-38 | So sánh token bằng `==` có thể rò rỉ timing khác nhau, hỗ trợ brute-force/side-channel. Với token nhạy cảm, attacker có thể đoán dần giá trị hợp lệ. [A02] |

### `main.py` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | SQL Injection | dòng 36 | `cursor.execute(f"SELECT * FROM profiles WHERE user_id = {u_id}")` ghép trực tiếp ID vào SQL. Nếu nguồn `u_id` bị ảnh hưởng hoặc kiểu dữ liệu bị phá, query có thể bị biến dạng và đọc sai dữ liệu. [A03] |

### `requirements.txt` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | MEDIUM | Outdated Component | dòng 3 | `requests==2.20.0` là version rất cũ; dependency stale làm tăng exposure tới CVE đã biết và giảm độ tin cậy supply chain. Đây là A06 nếu version này nằm trong production path. |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py, Dockerfile, auth.py, main.py, requirements.txt |
| Tổng finding | 11 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Nếu đây là code thật thì project đang exposed nghiêm trọng: có đường vào RCE, JWT bypass, SSRF và lộ secret. Mình khuyên chặn release ngay; nếu đây là demo intentional thì các lỗi này đúng kiểu để test scanner, nhưng vẫn nên tách khỏi code chạy production.
**⚠ 15 lỗi trên 4 file — project đang EXPOSED tới SQL injection, auth bypass, SSRF và secret leak**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` hoặc `&& curl ...` để RCE. Đây là A03:2021 [A03]. |
| 2 | CRITICAL | SQL Injection | dòng 28-29 | Query ghép trực tiếp `username` vào SQL; payload như `x' OR '1'='1' --` sẽ đổi logic WHERE và đọc toàn bộ bảng users. Đây là A03:2021 [A03]. |
| 3 | CRITICAL | Insecure Deserialization | dòng 77 | `pickle.loads(body)` nhận dữ liệu từ request body; attacker có thể gửi payload pickle độc hại để thực thi code khi deserialize. Đây là A08:2021 [A08]. |
| 4 | CRITICAL | JWT bypass | dòng 107 | `jwt.decode(..., options={"verify_signature": False})` bỏ qua xác thực chữ ký, nên token giả mạo có thể được chấp nhận. Ví dụ attacker sửa `username=admin` rồi tự ký không cần key. Đây là A07:2021 [A07]. |
| 5 | HIGH | XSS | dòng 48-49 | `return f"<html>...{name}..."` phản chiếu input vào HTML không escape; payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. Đây là A03:2021 [A03]. |
| 6 | HIGH | SSRF | dòng 98 | `requests.get(url)` fetch URL do user kiểm soát; attacker có thể trỏ tới `http://169.254.169.254/latest/meta-data/` hoặc localhost để đọc tài nguyên nội bộ. Đây là A10:2021 [A10]. |
| 7 | HIGH | SQL Injection | dòng 67 | `cursor.execute(f"SELECT * FROM accounts WHERE id = {account_id}")` cho phép chèn biểu thức SQL nếu tham số bị ép kiểu lỏng hoặc đi qua route khác; payload kiểu `1 OR 1=1` có thể mở rộng truy vấn. Đây là A03:2021 [A03]. |
| 8 | HIGH | Weak Hash | dòng 58-59 | `hashlib.md5(password.encode())` dùng MD5 cho mật khẩu; hash này dễ brute-force/rainbow-table, làm lộ credential nếu DB bị đọc. Đây là A02:2021 [A02]. |
| 9 | HIGH | Hardcoded Secret | dòng 56-57 | `SECRET_KEY` và `API_TOKEN` được hardcode trong source; nếu repo/bundle/log bị lộ thì attacker có thể dùng ngay để giả mạo hoặc truy cập dịch vụ. Đây là A02:2021 [A02]. |
| 10 | HIGH | Secret Logging | dòng 113-114 | `logging.info(... password={password})` ghi plaintext password vào log; ai đọc log là lấy được credential. Đây là exposure trực tiếp của secret. |

### `auth.py` (3 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 28-29 | `username` và `password` được nối thẳng vào query; payload như `admin' --` hoặc `' OR '1'='1` có thể bypass login và dump dữ liệu. Đây là A03:2021 [A03]. |
| 2 | CRITICAL | JWT bypass | dòng 42 | `jwt.decode(token, options={"verify_signature": False})` chấp nhận token không kiểm chữ ký; attacker có thể tự tạo payload `role=admin` và được tin tưởng. Đây là A07:2021 [A07]. |
| 3 | MEDIUM | Timing Attack | dòng 34-37 | So sánh token bằng `==` tạo side-channel timing nhỏ nhưng có thể bị khai thác trong ngữ cảnh nhạy cảm; attacker đo thời gian để suy ra giá trị từng byte. |

### `main.py` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | SQL Injection | dòng 36 | `cursor.execute(f"SELECT * FROM profiles WHERE user_id = {u_id}")` ghép ID trực tiếp vào SQL; nếu giá trị đầu vào bị thao túng ở upstream, attacker có thể mở rộng truy vấn và đọc profile người khác. Đây là A03:2021 [A03]. |

### `requirements.txt` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | MEDIUM | Outdated Component | dòng 3 | `requests==2.20.0` là phiên bản cũ và có thể kéo theo rủi ro CVE/bug đã biết; dependency stale làm tăng exposure supply-chain. Đây là A06:2021 [A06]. |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py, auth.py, main.py, requirements.txt |
| Tổng finding | 15 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Repo này không phải chỉ “có vài issue nhỏ” — nó đang exposed nghiêm trọng tới SQL injection, JWT bypass, command injection và secret leak. Nếu đây là code thật chứ không phải demo intentional, mình khuyên chặn release ngay cho tới khi các lỗi CRITICAL/HIGH được xử lý.
**⚠ 13 lỗi trên 4 file — project đang EXPOSED tới SQL injection, auth bypass, SSRF và secret leak**

### `auth.py` (4 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 28-29 | Query ghép chuỗi trực tiếp từ `username` và `password` vào SQL. Payload như `' OR '1'='1` có thể bypass đăng nhập và đọc dữ liệu users. [A03] |
| 2 | CRITICAL | Hardcoded Secret | dòng 20 | `SECRET_KEY` nằm thẳng trong source code, nên ai đọc repo cũng có thể dùng để ký token hoặc giả mạo session. Đây là exposure trực tiếp của bí mật thật. [A02] |
| 3 | HIGH | Timing Attack | dòng 34-37 | So sánh token bằng `==` tạo side-channel rò rỉ độ dài/khớp từng phần trong một số ngữ cảnh. Attacker có thể dò token hợp lệ bằng nhiều request lặp. [A02] |
| 4 | CRITICAL | JWT Bypass | dòng 42 | `jwt.decode(..., verify_signature=False)` chấp nhận payload không kiểm chữ ký, nên attacker có thể sửa `sub`/`role` rồi tự nhận là user khác. [A07] |

### `vulnerable_demo.py` (9 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 21-24 | `username` đi thẳng vào câu SQL qua f-string. Payload như `' OR '1'='1` có thể dump bảng `users` hoặc bypass auth. [A03] |
| 2 | CRITICAL | Command Injection | dòng 30-31 | `subprocess.check_output(..., shell=True)` với `host` từ user cho phép chèn `; cat /etc/passwd` để RCE. [A03] |
| 3 | HIGH | XSS | dòng 37-38 | `name` được nhúng thẳng vào HTML response. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] |
| 4 | CRITICAL | Hardcoded Secret | dòng 43-44 | `SECRET_KEY` và `API_TOKEN` là secret thật/giả mạo nằm trong source. Nếu đây là key live, attacker có thể dùng ngay để truy cập dịch vụ ngoài. [A02] |
| 5 | HIGH | Weak Hash | dòng 46-47 | `md5` là hàm băm yếu cho mật khẩu; attacker có thể crack nhanh bằng rainbow table hoặc GPU brute force. [A02] |
| 6 | HIGH | IDOR | dòng 54-57 | Truy vấn `account_id` trực tiếp từ path param mà không kiểm tra ownership/permission. Attacker đổi ID để đọc tài khoản người khác. [A01] |
| 7 | CRITICAL | Insecure Deserialization | dòng 64-67 | `pickle.loads(body)` trên dữ liệu từ request cho phép thực thi object payload độc hại. Payload pickle có thể dẫn tới RCE khi unpickle. [A08] |
| 8 | CRITICAL | Unsafe YAML Load | dòng 74-76 | `yaml.load(..., Loader=yaml.Loader)` có thể khởi tạo object tùy ý từ input. Payload YAML độc hại có thể dẫn tới code execution hoặc object injection. [A05] |
| 9 | CRITICAL | SSRF | dòng 82-84 | `requests.get(url)` fetch URL do user kiểm soát, nên attacker có thể gọi `http://169.254.169.254/latest/meta-data/` hoặc internal admin host. [A10] |
| 10 | CRITICAL | JWT Bypass | dòng 90-92 | `jwt.decode(..., verify_signature=False)` bỏ qua xác thực chữ ký, nên token giả mạo có thể tự đặt `username`/claims. [A07] |
| 11 | HIGH | Secret Logging | dòng 98-101 | Log thẳng `password` vào application logs làm lộ credential cho bất kỳ ai đọc log. Đây là exposure dữ liệu nhạy cảm qua monitoring. [A09] |

### `requirements.txt` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | Outdated Component | dòng 1-4 | `requests==2.20.0` và các pin cũ cho thấy dependency đã lỗi thời; nếu version này có CVE reachable, app sẽ kế thừa exploit path từ supply chain. [A06] |

### `migrations/001_risky_change.sql` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | MEDIUM | Dangerous Migration | dòng 1-4 | `DROP COLUMN` trực tiếp và `UPDATE` toàn bảng không batching có thể gây mất dữ liệu/lock kéo dài. Đây là exposure vận hành hơn là exploit trực tiếp. [A04] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | auth.py, vulnerable_demo.py, requirements.txt, migrations/001_risky_change.sql |
| Tổng finding | 13 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Nếu đây là code thật thì project đang exposed rất nặng: có SQL injection, command injection, SSRF, JWT bypass và secret leak cùng lúc. Mình khuyên chặn release ngay; bộ demo này rõ ràng được viết để test security, nhưng đưa vào production là rủi ro cực cao.
