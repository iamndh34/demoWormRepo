# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-08T11:30:19Z

**⚠ 10 lỗi trên 1 file — project đang exposed tới SQL injection, SSRF, XSS, IDOR và leak secret**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | Command Injection | dòng 29 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` hoặc `&& curl ...` để RCE. [A03] | `subprocess.run(["ping","-c","1",host], check=True, capture_output=True)` |
| 2 | CRITICAL | Insecure Deserialization | dòng 64 | `pickle.loads(body)` có thể bị lợi dụng để thực thi code khi attacker gửi payload pickle độc hại. Ví dụ payload có thể tạo object giả để chạy code lúc deserialize. [A08] | Dùng JSON an toàn: `obj = json.loads(body)` |
| 3 | HIGH | Hardcoded Secret | dòng 42-43 | `SECRET_KEY` và `API_TOKEN` hardcode trong source làm lộ credential nếu repo bị đọc hoặc leak. Ví dụ attacker lấy được token để truy cập tài nguyên bên ngoài. [A02] | Đưa vào env: `SECRET_KEY = os.environ["SECRET_KEY"]` |
| 4 | HIGH | SQL Injection | dòng 23-25 | Query nối chuỗi với `username` khiến payload như `' OR '1'='1` có thể dump toàn bộ bảng `users`. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 5 | HIGH | SQL Injection | dòng 55-57 | `account_id` được nhét thẳng vào SQL; dù kiểu `int`, pattern này dễ bị tái sử dụng sai và mở cửa cho injection khi input bị bypass ở tầng khác. Ví dụ `1 OR 1=1` nếu type enforcement không còn. [A03] | `cursor.execute("SELECT * FROM accounts WHERE id = ?", (account_id,))` |
| 6 | HIGH | SSRF | dòng 81-83 | `requests.get(url)` cho phép attacker bắt server gọi tới URL nội bộ như `http://169.254.169.254/latest/meta-data/` để đọc metadata hoặc quét mạng nội bộ. [A10] | Whitelist domain/scheme: `if not url.startswith("https://trusted/"): raise HTTPException(400)` |
| 7 | HIGH | JWT bypass | dòng 88-89 | `jwt.decode(..., verify_signature=False)` bỏ qua chữ ký, nên token tự ký hoặc sửa payload đều được chấp nhận. Ví dụ đổi `username` thành `admin` rồi gửi lại. [A07] | `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` |
| 8 | MEDIUM | XSS | dòng 37 | Trả HTML với `name` chưa escape cho phép payload như `<script>alert(1)</script>` chạy trong trình duyệt nạn nhân. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 9 | MEDIUM | Unsafe YAML Load | dòng 72 | `yaml.load(..., Loader=yaml.Loader)` có thể khởi tạo object độc hại; payload YAML crafted có thể dẫn tới arbitrary object construction. [A05] | `config = yaml.safe_load(body)` |
| 10 | MEDIUM | Secret Logging | dòng 98-101 | `logging.info` ghi thẳng password vào log; ai đọc log sẽ thấy credential rõ ràng. Ví dụ password `admin` bị lưu plaintext. [A09] | `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây đúng kiểu file demo cố ý cài lỗi để test. Nếu là code thật thì project đang exposed rất nặng và mình khuyên chặn release ngay cho tới khi xử lý hết các điểm CRITICAL/HIGH.
