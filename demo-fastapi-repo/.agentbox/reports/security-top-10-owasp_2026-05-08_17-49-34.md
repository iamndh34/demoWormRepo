# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-08T10:49:42Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQLi, RCE, XSS, SSRF, deserialization và lộ secret**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 28-29 | Query ghép trực tiếp `username` vào f-string rồi `cursor.execute(query)`. Attacker chỉ cần `username=' OR '1'='1` là có thể đọc toàn bộ bảng users. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(..., shell=True)` nhận `host` từ user nên payload kiểu `127.0.0.1; cat /etc/passwd` có thể dẫn tới RCE. [A03] | `subprocess.check_output(["ping","-c","1",host], shell=False)` |
| 3 | HIGH | XSS | dòng 47 | Trả HTML trực tiếp với `name` chưa escape. Payload như `<script>alert(1)</script>` sẽ chạy ngay trên trình duyệt nạn nhân. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 4 | CRITICAL | Secret Exposure | dòng 53-54 | Hardcode `SECRET_KEY` và `API_TOKEN` trong source là lộ bí mật thật nếu repo bị leak. `AKIA...` còn giống AWS key pattern, attacker có thể dùng để truy cập tài nguyên. [A02] | Đưa vào env/secret manager: `SECRET_KEY = os.getenv("SECRET_KEY")` |
| 5 | HIGH | Weak Hash | dòng 57 | Dùng `hashlib.md5()` để hash password là yếu, dễ bị brute-force/rainbow table. Password `123456` có thể bị crack rất nhanh. [A02] | `import hashlib, secrets` rồi dùng `bcrypt/argon2`; tối thiểu `hashlib.sha256(...)` không đủ cho password |
| 6 | HIGH | IDOR | dòng 67 | Lấy account theo `account_id` mà không kiểm tra owner/authorization. Attacker đổi `/account/1` → `/account/2` là đọc dữ liệu tài khoản khác. [A01] | Check quyền trước khi query: `if account_id != current_user.id: raise HTTPException(403)` |
| 7 | CRITICAL | Insecure Deserialization | dòng 77 | `pickle.loads(body)` trên dữ liệu từ request là cực nguy hiểm; payload pickle độc hại có thể thực thi code khi deserialize. [A08] | Dùng JSON an toàn: `obj = json.loads(body)` |
| 8 | HIGH | Unsafe YAML Load | dòng 89 | `yaml.load(..., Loader=yaml.Loader)` cho phép object construction nguy hiểm; payload YAML có thể dẫn tới RCE hoặc đọc object ngoài ý muốn. [A05] | `config = yaml.safe_load(body)` |
| 9 | CRITICAL | SSRF | dòng 98 | `requests.get(url)` nhận URL từ user nên attacker có thể gọi nội bộ như `http://127.0.0.1:8000/admin` hoặc metadata service cloud. [A10] | Whitelist domain/scheme rồi chặn IP nội bộ trước khi fetch |
| 10 | HIGH | JWT Bypass | dòng 107-108 | `jwt.decode(..., verify_signature=False)` bỏ xác thực chữ ký, attacker tự tạo token với `{"username":"admin"}` là giả danh được. [A07] | `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` |



---

## Pass 2: Language Idiom Check + Red Team

**✅ 0 lỗi mới trên 1 file — mình không thấy finding nào khớp với pass 1/2 yêu cầu mới trong `vulnerable_demo.py`**

### `vulnerable_demo.py` (0 lỗi mới)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | INFO | Không có finding mới | dòng 1-122 | Mình đã rà đúng các nhóm bạn yêu cầu cho 2 section mới: unchecked error returns, username enumeration, missing file type validation, race conditions, business logic flaws, DoS vectors, và SSRF via format parameter. Trong file này không có `_, err :=`/ignored return kiểu Go, không có luồng đăng nhập với thông báo khác nhau, không có upload handler, không thấy stateful payment flow để tạo race, và cũng không có export `format` parameter nên chưa có finding mới đáng báo. | `# Không cần sửa cho pass này` |




---

## Pass 3: CVSS + CWE + OWASP Coverage Matrix

**📋 10 lỗi trên 1 file — project đang EXPOSED tới đủ bộ OWASP Top 10 ở `vulnerable_demo.py`**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 28-29 | Query ghép trực tiếp `username` vào SQL, attacker chỉ cần `username = `' OR '1'='1` để dump toàn bộ bảng `users`. Đây là A03:2021 và có thể dẫn tới đọc/sửa/xóa dữ liệu trái phép. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(..., shell=True)` với `host` từ user cho phép chèn `; cat /etc/passwd` hoặc `&& curl ...` để RCE. Đây là đường vào thực thi lệnh từ xa rất nguy hiểm. [A03] | `subprocess.check_output(["ping","-c","1",host], shell=False)` |
| 3 | CRITICAL | XSS | dòng 47 | HTML trả thẳng `name` vào response, attacker có thể gửi `<script>alert(1)</script>` để chiếm session, lừa người dùng hoặc pivot sang CSRF. Đây là phản xạ XSS điển hình. [A03] | `return HTMLResponse(f"<h1>Hello, {html.escape(name)}!</h1>")` |
| 4 | CRITICAL | Hardcoded Credentials | dòng 53-54 | `SECRET_KEY` và `API_TOKEN` bị hardcode trong source. Nếu repo bị lộ, attacker có thể dùng secret để ký token giả, truy cập tài nguyên cloud, hoặc impersonate dịch vụ. [A02] | `SECRET_KEY = os.getenv("SECRET_KEY")` |
| 5 | HIGH | Weak Hash | dòng 57 | Dùng `md5` để hash password là không an toàn; attacker có thể rainbow-table hoặc brute-force rất nhanh. Ví dụ mật khẩu yếu sẽ bị crack offline chỉ trong thời gian ngắn. [A02] | `return hashlib.sha256(password.encode()).hexdigest()` |
| 6 | CRITICAL | IDOR | dòng 67 | `account_id` được dùng thẳng để truy vấn record mà không kiểm tra quyền sở hữu. Attacker chỉ cần đổi `/account/1`, `/account/2` để xem tài khoản người khác. Đây là Broken Access Control, A01:2021. [A01] | `cursor.execute("SELECT * FROM accounts WHERE id=? AND owner_id=?", (account_id, user_id))` |
| 7 | CRITICAL | Insecure Deserialization | dòng 77 | `pickle.loads(body)` cho phép payload độc hại thực thi code khi deserialize. Payload giả mạo có thể bật reverse shell hoặc đọc file nội bộ ngay lúc parse. [A08] | `obj = json.loads(body)` |
| 8 | HIGH | Unsafe YAML Load | dòng 89 | `yaml.load(..., Loader=yaml.Loader)` có thể kích hoạt object construction nguy hiểm; payload YAML crafted có thể dẫn tới RCE/DoS. Đây là unsafe deserialization/misconfig. [A05] | `config = yaml.safe_load(body)` |
| 9 | HIGH | SSRF | dòng 98 | `requests.get(url)` lấy URL từ user cho phép quét nội bộ như `http://127.0.0.1:8000/`, metadata cloud, hoặc pivot vào mạng private. [A10] | `assert url.startswith("https://your-domain/"); requests.get(url, timeout=3)` |
| 10 | HIGH | JWT Bypass | dòng 107 | `jwt.decode(..., verify_signature=False)` bỏ qua xác thực chữ ký, attacker có thể tự sửa payload `"username":"admin"` rồi gửi token giả mạo. Đây là Authentication Failure, A07:2021. [A07] | `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` |

### OWASP Top 10 Coverage Matrix

| OWASP 2021 | Số lượng finding | Ví dụ file:line |
|---|---:|---|
| A01: Broken Access Control | 1 | `vulnerable_demo.py:67` |
| A02: Cryptographic Failures | 2 | `vulnerable_demo.py:53-57` |
| A03: Injection | 3 | `vulnerable_demo.py:28-29`, `vulnerable_demo.py:38`, `vulnerable_demo.py:47` |
| A04: Insecure Design | 0 | N/A |
| A05: Security Misconfiguration | 1 | `vulnerable_demo.py:84,89` |
| A06: Vulnerable and Outdated Components | 0 | N/A |
| A07: Identification and Authentication Failures | 1 | `vulnerable_demo.py:107` |
| A08: Software and Data Integrity Failures | 1 | `vulnerable_demo.py:77` |
| A09: Security Logging and Monitoring Failures | 0 | N/A |
| A10: Server-Side Request Forgery | 1 | `vulnerable_demo.py:98` |

### CWE References

| # | CWE ID | Name | Finding | File:line |
|---:|---|---|---|---|
| 1 | CWE-89 | SQL Injection | SQLi | `vulnerable_demo.py:28-29` |
| 2 | CWE-78 | OS Command Injection | Command Injection | `vulnerable_demo.py:38` |
| 3 | CWE-79 | Cross-Site Scripting | XSS | `vulnerable_demo.py:47` |
| 4 | CWE-798 | Use of Hard-coded Credentials | Hardcoded Credentials | `vulnerable_demo.py:53-54` |
| 5 | CWE-328 | Use of Weak Hash | Weak Hash | `vulnerable_demo.py:57` |
| 6 | CWE-639 | Authorization Bypass Through User-Controlled Key | IDOR | `vulnerable_demo.py:67` |
| 7 | CWE-502 | Deserialization of Untrusted Data | Insecure Deserialization | `vulnerable_demo.py:77` |
| 8 | CWE-915 | Improperly Controlled Modification of Dynamically-Determined Object Attributes | Unsafe YAML Load | `vulnerable_demo.py:89` |
| 9 | CWE-918 | Server-Side Request Forgery | SSRF | `vulnerable_demo.py:98` |
| 10 | CWE-347 | Improper Verification of Cryptographic Signature | JWT Bypass | `vulnerable_demo.py:107` |
| 11 | CWE-532 | Insertion of Sensitive Information into Log File | Logging secrets | `vulnerable_demo.py:118` |
| 12 | CWE-319 | Cleartext Transmission of Sensitive Information | Credential exposure in logs | `vulnerable_demo.py:118` |
| 13 | CWE-306 | Missing Authentication for Critical Function | Missing auth pattern | `vulnerable_demo.py:105-108` |
| 14 | CWE-20 | Improper Input Validation | Unvalidated `host` / `url` | `vulnerable_demo.py:37,97` |
| 15 | CWE-200 | Exposure of Sensitive Information to an Unauthorized Actor | Secrets in source | `vulnerable_demo.py:53-54` |

### CVSS v3.1 Scores

| # | Finding | CVSS v3.1 | Vector |
|---:|---|---:|---|
| 1 | SQL Injection | 9.8 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H` |
| 2 | Command Injection | 9.8 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H` |
| 3 | XSS | 8.8 | `AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:L` |
| 4 | Hardcoded Credentials | 9.1 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H` |
| 5 | Weak Hash | 7.5 | `AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:L/A:N` |
| 6 | IDOR | 8.8 | `AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:L` |
| 7 | Insecure Deserialization | 9.8 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H` |
| 8 | Unsafe YAML Load | 8.8 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H` |
| 9 | SSRF | 8.6 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:L` |
| 10 | JWT Bypass | 9.1 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N` |
| 11 | Logging secrets | 6.5 | `AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:L/A:N` |
| 12 | Cleartext secret logging | 6.5 | `AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:L/A:N` |
| 13 | Missing auth | 8.1 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N` |
| 14 | Improper input validation | 6.1 | `AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:L/A:L` |
| 15 | Sensitive info exposure | 7.5 | `AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N` |

### Compliance Mapping

| Finding | GDPR Article | PCI-DSS Requirement | SOC 2 Control |
|---|---|---|---|
| SQL Injection | Art. 5(1)(f), Art. 32 | 6.2, 6.5 | CC6.1, CC7.2 |
| Command Injection | Art. 32 | 6.2, 6.5 | CC6.1, CC7.2 |
| XSS | Art. 32 | 6.5 | CC6.1, CC7.2 |
| Hardcoded Credentials | Art. 32 | 3.5, 6.3 | CC6.1, CC6.7 |
| Weak Hash | Art. 32 | 3.4, 8.3 | CC6.1, CC6.6 |
| IDOR | Art. 5(1)(f), Art. 32 | 7.1, 7.2 | CC6.1, CC6.2 |
| Insecure Deserialization | Art. 32 | 6.5 | CC7.1, CC7.2 |
| Unsafe YAML Load | Art. 32 | 6.5 | CC7.1, CC7.2 |
| SSRF | Art. 32 | 6.5 | CC6.1, CC7.2 |
| JWT Bypass | Art. 32 | 8.1, 8.3 | CC6.1, CC6.2 |

### False Positive Analysis

| Kết luận | Số lượng | Lý do |
|---|---:|---|
| Confirmed true positives | 10 | Tất cả finding đều thấy trực tiếp trong code, có sink rõ ràng và payload khai thác thực tế được. |
| Potential false positives | 0 | Không có pattern mơ hồ; mỗi issue đều có trace input → sink rõ ràng, line number cụ thể. |

### Fix Priority Matrix

| Thứ tự | Finding | CVSS | Effort | Ưu tiên | Gợi ý xử lý |
|---:|---|---:|---|---|---|
| 1 | SQL Injection | 9.8 | Low | P0 | Parameterize query, không nối chuỗi SQL |
| 2 | Command Injection | 9.8 | Low | P0 | Bỏ `shell=True`, whitelist host hợp lệ |
| 3 | Insecure Deserialization | 9.8 | Medium | P0 | Thay `pickle` bằng JSON / schema an toàn |
| 4 | Hardcoded Credentials | 9.1 | Low | P0 | Chuyển sang env/secret manager, rotate secret |
| 5 | JWT Bypass | 9.1 | Low | P0 | Bắt buộc verify chữ ký + algorithm allowlist |
| 6 | XSS | 8.8 | Low | P1 | Escape output, template auto-escaping |
| 7 | IDOR | 8.8 | Medium | P1 | Check ownership/authorization trước query |
| 8 | Unsafe YAML Load | 8.8 | Low | P1 | Dùng `yaml.safe_load` |
| 9 | SSRF | 8.6 | Medium | P1 | Allowlist domain, chặn private IP/metadata |
| 10 | Missing auth | 8.1 | Medium | P1 | Thêm authz layer, validate token/permissions |
| 11 | Sensitive info exposure | 7.5 | Low | P2 | Xóa log mật khẩu, mask PII |
| 12 | Weak Hash | 7.5 | Low | P2 | Dùng Argon2/bcrypt/scrypt thay MD5 |
| 13 | Logging secrets | 6.5 | Low | P2 | Redact secrets trong log |
| 14 | Cleartext secret logging | 6.5 | Low | P2 | Không log password, dùng structured redaction |
| 15 | Improper input validation | 6.1 | Medium | P2 | Validate/normalize `host`, `url`, `username` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|---|---|
| File bị ảnh hưởng | `vulnerable_demo.py` |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |
| Mẫu tấn công nổi bật | SQLi `' OR '1'='1`, command injection `; cat /etc/passwd`, XSS `<script>alert(1)</script>`, JWT giả mạo với `verify_signature=False` |
| Kết luận | Mã demo này cố ý chứa lỗi để test; nếu đây là code thật thì project đang exposed nghiêm trọng và cần chặn release ngay |

