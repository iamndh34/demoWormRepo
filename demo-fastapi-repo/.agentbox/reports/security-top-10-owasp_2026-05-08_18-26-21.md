# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-08T11:26:35Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQL injection, RCE, XSS, SSRF, JWT bypass và lộ secrets.**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | Command Injection | dòng 29 | `subprocess.check_output(..., shell=True)` ghép trực tiếp `host` từ user. Attacker chỉ cần `8.8.8.8; cat /etc/passwd` là có thể chạy lệnh tùy ý trên máy chủ. [A03] | Dùng args list và `shell=False`: `subprocess.check_output(["ping","-c","1", host], shell=False)` |
| 2 | CRITICAL | Insecure Deserialization | dòng 50-51 | `pickle.loads(body)` deserialize dữ liệu do user gửi lên. Payload pickle độc hại có thể kích hoạt RCE ngay khi parse body. [A08] | Tránh `pickle`; nếu bắt buộc, dùng format an toàn như JSON + schema validation. |
| 3 | CRITICAL | SSRF | dòng 70-71 | `requests.get(url)` lấy URL từ user mà không giới hạn host/protocol. Attacker có thể hit metadata service như `http://169.254.169.254/latest/meta-data/` để đọc secrets nội bộ. [A10] | Allowlist domain/protocol và chặn IP nội bộ trước khi gọi request. |
| 4 | HIGH | SQL Injection | dòng 24-26 | Query f-string nhúng `username` trực tiếp vào SQL. Ví dụ `username = `' OR '1'='1` sẽ làm query trả toàn bộ bảng users. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 5 | HIGH | SQL Injection | dòng 42-44 | `account_id` được chèn thẳng vào query bằng f-string; nếu input bị kiểm soát qua route/typing bypass, attacker có thể sửa điều kiện WHERE để đọc record khác. [A03] | `cursor.execute("SELECT * FROM accounts WHERE id = ?", (account_id,))` |
| 6 | HIGH | XSS | dòng 35-36 | Trả HTML bằng f-string với `name` chưa escape. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | Escape output: `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 7 | HIGH | JWT Bypass | dòng 77-78 | `jwt.decode(..., verify_signature=False)` bỏ qua kiểm chữ ký, nên token giả mạo với `{"username":"admin"}` sẽ được chấp nhận. [A07] | `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` và kiểm `exp`, `iss`. |
| 8 | HIGH | Hardcoded Secret | dòng 44-45 | `SECRET_KEY` và `API_TOKEN` hardcode trong source; nếu repo lộ thì attacker tái sử dụng để ký token hoặc truy cập dịch vụ. [A02] | Đưa secrets sang env/secret manager và rotate ngay. |
| 9 | MEDIUM | Weak Hash | dòng 47-48 | `md5(password)` là hash yếu, dễ bị rainbow-table và brute force. Với mật khẩu phổ biến như `admin123`, attacker có thể khôi phục rất nhanh. [A02] | Dùng `bcrypt`/`argon2` thay cho MD5. |
| 10 | MEDIUM | Secret Logging | dòng 85-86 | Log thẳng `password` trong `logging.info(...)` làm credentials rơi vào log. Bất kỳ ai đọc log đều có thể lấy lại mật khẩu đăng nhập. [A09] | Chỉ log username và mask password: `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |
| Kết luận | Đây là file demo cố tình chứa lỗi, nhưng mức exposure vẫn rất cao; nếu là code thật thì mình khuyên block release ngay. |
