# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T05:21:33Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQL injection, RCE, XSS, SSRF, deserialization và rò rỉ secrets**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 23-25 | Query ghép f-string trực tiếp với `username`; attacker có thể dùng `name=' OR '1'='1` để dump toàn bộ bảng users. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 31-32 | `subprocess.check_output(..., shell=True)` nhận `host` từ user; payload như `8.8.8.8; cat /etc/passwd` có thể dẫn tới RCE. [A03] | `subprocess.check_output(["ping","-c","1", host], shell=False)` |
| 3 | HIGH | XSS | dòng 38-39 | Trả HTML với `name` chưa escape; attacker có thể chèn `<script>alert(1)</script>` để chạy script trên trình duyệt nạn nhân. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 4 | HIGH | Hardcoded Secret | dòng 44-45 | `SECRET_KEY` và `API_TOKEN` hardcode trong source; nếu repo lộ, attacker dùng ngay để ký token hoặc truy cập dịch vụ liên quan. [A02] | Đưa secret sang env: `SECRET_KEY = os.getenv("SECRET_KEY")` |
| 5 | HIGH | IDOR | dòng 52-55 | `account_id` lấy thẳng từ path và truy vấn theo ID, không có kiểm tra ownership; attacker đổi `/account/2` để xem tài khoản người khác. [A01] | `cursor.execute("SELECT * FROM accounts WHERE id = ? AND owner_id = ?", (account_id, current_user.id))` |
| 6 | CRITICAL | Insecure Deserialization | dòng 61-63 | `pickle.loads(body)` giải mã dữ liệu do user gửi; payload pickle độc có thể thực thi code khi load. [A08] | Chỉ nhận JSON: `obj = json.loads(body)` hoặc dùng schema an toàn |
| 7 | HIGH | Unsafe YAML Load | dòng 69-71 | `yaml.load(..., Loader=yaml.Loader)` cho phép object construction nguy hiểm; payload YAML có thể kích hoạt RCE/DoS. [A05] | `config = yaml.safe_load(body)` |
| 8 | HIGH | SSRF | dòng 78-80 | `requests.get(url)` fetch URL do user cung cấp; attacker có thể gọi metadata/internal service như `http://169.254.169.254/`. [A10] | Whitelist domain/scheme: `assert url.startswith("https://trusted.example/")` |
| 9 | HIGH | JWT Bypass | dòng 86-87 | `jwt.decode(..., verify_signature=False)` bỏ kiểm tra chữ ký; attacker sửa payload rồi đổi `username` thành admin. [A07] | `jwt.decode(token, SECRET_KEY, algorithms=["HS256"])` |
| 10 | MEDIUM | Secret Logging | dòng 93-97 | Log trực tiếp `password`; ai đọc log có thể lấy credential, ví dụ payload `/login?password=admin`. [A09] | `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là demo cố ý chứa lỗi, nhưng nếu là code thật thì project đang exposed rất nặng và nên chặn release ngay. Các lỗi CRITICAL như SQL injection, command injection và insecure deserialization có thể dẫn tới mất dữ liệu hoặc RCE.
