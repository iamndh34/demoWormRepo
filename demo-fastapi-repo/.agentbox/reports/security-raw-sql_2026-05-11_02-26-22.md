# security-raw-sql — saved by agentbox --save

Generated: 2026-05-10T19:26:34Z

**⚠ 3 vấn đề trên 2 file — dự án đang bị lộ SQL injection qua raw SQL nội suy chuỗi**

### `auth.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | NGHIÊM TRỌNG | SQL Injection | line 28-29 | `username` và `password` được chèn thẳng vào chuỗi SQL trước khi gọi `cursor.execute()`. Kẻ tấn công có thể sửa cú pháp truy vấn, ví dụ biến `WHERE` thành điều kiện luôn đúng để bypass đăng nhập và đọc dữ liệu trái phép. [A03] |

### `vulnerable_demo.py` (2 issues)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | NGHIÊM TRỌNG | SQL Injection | line 28-29 | `username` bị nối trực tiếp vào chuỗi SQL rồi đem đi execute. Đây là exposure rõ ràng: input độc hại có thể làm đổi cấu trúc truy vấn và lấy ra toàn bộ bản ghi. [A03] |
| 2 | NGHIÊM TRỌNG | SQL Injection | line 67 | `account_id` được nhúng bằng f-string vào câu SQL. Dù trông giống số, nếu đầu vào không được kiểm tra chặt, nó vẫn có thể làm thay đổi cú pháp query và dẫn tới đọc/sửa dữ liệu ngoài ý muốn. [A03] |

### Summary notes

| Item | Content |
|------|---------|
| Files affected | auth.py, vulnerable_demo.py |
| Total findings | 3 |
| Highest severity | CRITICAL |

### Conclusion

Code có vẻ là demo cố ý, nhưng các sink raw SQL này là nguy cơ thật nếu chúng nằm trong đường chạy thực tế. Mình khuyên chặn release cho tới khi thay các query nội suy bằng tham số hóa hoặc allowlist chặt.
