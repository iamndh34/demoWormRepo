# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-14T09:07:41Z

**⚠ 14 lỗi trên 5 file — project đang EXPOSED tới SQL injection, JWT bypass, SSRF và secret leak**

### `vulnerable_demo.py` (8 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` để RCE trực tiếp. Đây là A03:2021 và có thể dẫn tới chiếm quyền máy chủ. |
| 2 | CRITICAL | SQL Injection | dòng 22-24 | Query ghép f-string với `username` đi thẳng vào `cursor.execute(query)`. Payload như `' OR '1'='1` có thể bypass đăng nhập và đọc/sửa dữ liệu. [A03] |
| 3 | CRITICAL | Insecure Deserialization | dòng 77 | `pickle.loads(body)` nhận dữ liệu từ request body nên attacker có thể gửi payload pickle độc để thực thi code khi deserialize. Đây là A08 và thường dẫn tới RCE. |
| 4 | CRITICAL | JWT bypass | dòng 107 | `jwt.decode(..., options={"verify_signature": False})` bỏ qua xác thực chữ ký, nên token giả mạo có thể được chấp nhận. Attacker chỉ cần sửa payload để mạo danh user khác. [A07] |
| 5 | HIGH | SSRF | dòng 98 | `requests.get(url)` lấy URL từ user mà không chặn localhost/private IP hay redirect chain. Payload như `http://169.254.169.254/latest/meta-data/` có thể lộ metadata cloud. [A10] |
| 6 | HIGH | Hardcoded Secret | dòng 53-54 | `SECRET_KEY` và `API_TOKEN` được hardcode trong source. Nếu repo hoặc image bị lộ, attacker có thể dùng secret thật để ký token hoặc truy cập dịch vụ ngoài. [A02] |
| 7 | HIGH | Weak Hash | dòng 57 | `hashlib.md5(...)` là hash yếu cho mật khẩu; attacker có thể brute-force/rainbow table nhanh hơn nhiều so với hash hiện đại. [A02] |
| 8 | MEDIUM | XSS | dòng 31-33 | Trả HTML bằng f-string với `name` chưa escape. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] |

### `auth.py` (3 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 29 | `cursor.execute(query)` chạy query đã ghép chuỗi từ `username`/`password`. Payload kiểu `' OR 1=1 --` có thể bypass auth và dump bảng users. [A03] |
| 2 | CRITICAL | JWT bypass | dòng 42 | `jwt.decode(token, options={"verify_signature": False})` chấp nhận token không cần chữ ký hợp lệ. Attacker có thể tự sửa `sub`/`role` để mạo danh. [A07] |
| 3 | HIGH | Hardcoded Secret | dòng 20 | `SECRET_KEY = "super-secret-key-12345"` là secret thật nằm ngay trong code. Nếu lộ source, toàn bộ cơ chế ký/giải mã phụ thuộc vào key này bị phá. [A02] |

### `main.py` (2 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | SQL Injection | dòng 36 | `cursor.execute(f"SELECT * FROM profiles WHERE user_id = {u_id}")` ghép trực tiếp ID vào SQL. Nếu `u_id` bị kiểm soát hoặc bị nhiễm từ nguồn khác, attacker có thể sửa logic truy vấn. [A03] |
| 2 | MEDIUM | SSRF | dòng 44 | `requests.get("https://slow-api.com/data")` là outbound call trong `async def`; hiện tại URL cố định nên chưa phải SSRF trực tiếp, nhưng nếu sau này đổi sang input người dùng thì sẽ mở đường cho SSRF. |

### `requirements.txt` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | Vulnerable Component | dòng 3 | `requests==2.20.0` là dependency rất cũ và có thể kéo theo CVE đã biết trong chuỗi supply chain. Project đang exposed tới rủi ro từ component lỗi thời. [A06] |

### `Dockerfile` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Secret Leak | dòng 14 | `ARG GITHUB_TOKEN=ghp_secret_123456789` đưa secret vào build context và docker history. Nếu image hoặc layer bị truy cập, token có thể bị lộ và bị lạm dụng ngay. [A02] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py, auth.py, main.py, requirements.txt, Dockerfile |
| Tổng finding | 14 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Mã này rõ ràng là demo cố ý chứa lỗi để test scanner. Nếu đây là code thật thì project đang exposed nghiêm trọng ở nhiều mặt: injection, auth bypass, SSRF và secret leak. Mình sẽ chặn release ngay cho tới khi các đường tấn công này được xử lý.
