# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T06:00:40Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQL injection, RCE, SSRF và bypass xác thực**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 22-24 | Query ghép chuỗi trực tiếp với `username`; attacker nhập `x' OR '1'='1` là có thể đọc toàn bộ bảng users. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 31 | `shell=True` + f-string với `host` cho phép chèn `; cat /etc/passwd` để chạy lệnh tùy ý. [A03] | `subprocess.check_output(["ping","-c","1", host], shell=False)` |
| 3 | HIGH | XSS | dòng 38-39 | Trả HTML chứa trực tiếp `name`; payload như `<script>alert(1)</script>` sẽ chạy trên trình duyệt người dùng. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 4 | HIGH | Weak Hash | dòng 48-49 | Dùng `md5` để hash mật khẩu là cực yếu; attacker bẻ khóa rất nhanh bằng rainbow table / brute force. [A02] | `from passlib.context import CryptContext; pwd=CryptContext(schemes=["bcrypt"]).hash(password)` |
| 5 | HIGH | Hardcoded Secret | dòng 46-47 | `SECRET_KEY` và `API_TOKEN` hardcode trong source; nếu repo lộ, attacker có thể ký token hoặc dùng thẳng credential. [A02] | `SECRET_KEY = os.environ["SECRET_KEY"]` |
| 6 | HIGH | IDOR | dòng 54-57 | Endpoint `/account/{account_id}` không có kiểm tra quyền sở hữu; đổi `account_id=2` là đọc record của người khác. [A01] | `if account_id != current_user.id: raise HTTPException(403)` |
| 7 | CRITICAL | Insecure Deserialization | dòng 64-66 | `pickle.loads(body)` trên dữ liệu user-controlled có thể dẫn tới RCE; payload pickle độc hại có thể thực thi khi deserialize. [A08] | `obj = json.loads(body)` |
| 8 | HIGH | Unsafe YAML Load | dòng 73-75 | `yaml.load(..., Loader=yaml.Loader)` cho phép object construction; payload YAML có thể trigger code path nguy hiểm. [A05] | `config = yaml.safe_load(body)` |
| 9 | HIGH | SSRF | dòng 82-84 | `requests.get(url)` tin tưởng URL từ user; attacker có thể gọi `http://169.254.169.254/...` để chạm metadata/internal service. [A10] | `if urlparse(url).hostname not in ALLOWED_HOSTS: raise ValueError()` |
| 10 | CRITICAL | JWT Bypass | dòng 91-93 | `jwt.decode(..., verify_signature=False)` bỏ qua chữ ký; attacker tự sửa payload thành admin rồi đăng nhập giả. [A07] | `jwt.decode(token, JWT_SECRET, algorithms=["HS256"], options={"verify_signature": True})` |

### `vulnerable_demo.py` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | HIGH | Log Secret | dòng 99-101 | Log thẳng `password` ra file/console; ai có quyền xem log là lấy được credential, đặc biệt nguy hiểm khi log tập trung. [A09] | `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Mã này là demo cố ý chứa lỗi nên các finding là hợp lý; nếu đây là code thật thì project đang exposed rất nặng và nên chặn release ngay. Ưu tiên fix các lỗi CRITICAL trước vì chúng có thể dẫn tới SQLi, RCE và bypass xác thực trực tiếp.
