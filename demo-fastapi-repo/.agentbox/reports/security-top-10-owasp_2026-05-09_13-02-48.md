# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T06:03:04Z

**⚠ 10 lỗi trên 1 file — project đang exposed tới SQL injection, RCE, SSRF và auth bypass**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | Injection | dòng 24-26 | Query ghép f-string với `username` đi thẳng vào SQL: attacker chỉ cần nhập `' OR '1'='1` là có thể đọc toàn bộ bảng `users`. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Injection | dòng 32-33 | `subprocess.check_output(..., shell=True)` với `host` do user kiểm soát cho phép chèn `; cat /etc/passwd` hoặc lệnh khác để RCE. [A03] | `subprocess.check_output(["ping", "-c", "1", host], shell=False)` |
| 3 | HIGH | XSS | dòng 39-40 | `name` được nhúng thẳng vào HTML trả về. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | `from html import escape; return f"<html>...{escape(name)}..."` |
| 4 | HIGH | Cryptographic Failures | dòng 45-49 | `SECRET_KEY` và `API_TOKEN` hardcoded trong source, lại còn hash password bằng `md5` — attacker lấy được secret từ repo và bẻ mật khẩu rất nhanh. [A02] | `SECRET_KEY=os.getenv("SECRET_KEY"); from passlib.context import CryptContext` |
| 5 | HIGH | Broken Access Control | dòng 54-57 | Endpoint đọc `accounts/{account_id}` không kiểm tra ownership/role. Chỉ cần đổi `account_id=2` là có thể xem tài khoản người khác. [A01] | `if not can_view_account(current_user, account_id): raise HTTPException(403)` |
| 6 | CRITICAL | Insecure Deserialization | dòng 63-66 | `pickle.loads(body)` trên dữ liệu từ request là đường thẳng tới thực thi code khi attacker gửi payload pickle độc hại. [A08] | `obj = json.loads(body)` hoặc schema `pydantic` thay cho `pickle` |
| 7 | HIGH | Security Misconfiguration | dòng 70-77 | `DEBUG = True` và `yaml.load(..., Loader=yaml.Loader)` đều nguy hiểm: debug có thể lộ stack trace, còn YAML loader không an toàn có thể bị lợi dụng để deserialize object ngoài ý muốn. [A05] | `DEBUG=False; config = yaml.safe_load(body)` |
| 8 | CRITICAL | SSRF | dòng 82-85 | Backend fetch URL do user cung cấp bằng `requests.get(url)` không allowlist. Attacker có thể ép server gọi `http://169.254.169.254/` hoặc tài nguyên nội bộ. [A10] | `parsed=urlparse(url); assert parsed.hostname in ALLOWED_HOSTS` |
| 9 | CRITICAL | JWT bypass | dòng 90-92 | `jwt.decode(..., verify_signature=False)` bỏ qua xác minh chữ ký, nên attacker có thể sửa payload token và tự nhận là `admin`. [A07] | `jwt.decode(token, key=JWT_SECRET, algorithms=["HS256"], options={"verify_signature": True})` |
| 10 | MEDIUM | Security Logging & Monitoring Failures | dòng 97-101 | Log đang ghi thẳng mật khẩu vào file log. Nếu log bị đọc lén hoặc ship sang hệ thống khác, credentials sẽ bị lộ; attacker chỉ cần login thử là để lại dấu vết nhạy cảm. [A09] | `logging.info("Login attempt: user=%s", username)` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý chứa lỗi, nên trạng thái này phù hợp cho testing. Nếu đây là code thật thì project đang exposed rất nặng với các lỗi CRITICAL và mình khuyên chặn release ngay.
