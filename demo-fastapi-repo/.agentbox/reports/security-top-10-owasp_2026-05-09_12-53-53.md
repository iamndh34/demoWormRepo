# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-09T05:54:08Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới SQL injection, RCE, SSRF, auth bypass và lộ secret**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả | Gợi ý cách sửa |
|----:|--------------------|----------|--------|----------------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 21-24 | `username` đi thẳng vào f-string SQL rồi `cursor.execute(query)`. Attacker chỉ cần nhập `x' OR '1'='1` để dump toàn bộ bảng `users`. [A03] | `cursor.execute("SELECT * FROM users WHERE username = ?", (username,))` |
| 2 | CRITICAL | Command Injection | dòng 30-31 | `host` được nhét vào `shell=True`; payload như `127.0.0.1; cat /etc/passwd` có thể dẫn tới RCE. [A03] | `subprocess.check_output(["ping","-c","1",host], shell=False)` |
| 3 | HIGH | XSS | dòng 37-39 | `name` bị phản chiếu thẳng vào HTML không escape. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] | `from html import escape; return f"<h1>Hello, {escape(name)}!</h1>"` |
| 4 | HIGH | Cryptographic Failures | dòng 45-49 | `SECRET_KEY` và `API_TOKEN` hardcoded trong source; kèm `hashlib.md5()` để hash password là yếu và đoán được. Nếu lộ repo là lộ luôn bí mật. [A02] | Dùng secret từ env + `CryptContext(schemes=["bcrypt"])` |
| 5 | HIGH | IDOR | dòng 54-58 | `/account/{account_id}` trả dữ liệu theo ID path parameter nhưng không thấy kiểm tra ownership/role. Attacker đổi `account_id=2` là đọc account người khác. [A01] | `if not can_view_account(current_user, account_id): raise HTTPException(403)` |
| 6 | CRITICAL | Insecure Deserialization | dòng 63-66 | `pickle.loads(body)` trên dữ liệu từ request là cực nguy hiểm; payload pickle độc hại có thể thực thi code khi deserialize. [A08] | Chỉ nhận JSON: `obj = await request.json()` hoặc schema Pydantic |
| 7 | HIGH | Security Misconfiguration | dòng 70-75 | `DEBUG = True` trong app production và `yaml.load(..., Loader=yaml.Loader)` mở cửa cho unsafe object construction; payload YAML crafted có thể gây hành vi ngoài ý muốn. [A05] | `DEBUG = False`; `yaml.safe_load(body)` |
| 8 | CRITICAL | SSRF | dòng 80-83 | `requests.get(url)` fetch URL do user cung cấp; attacker có thể gọi nội bộ như `http://169.254.169.254/latest/meta-data/` để lộ metadata. [A10] | Allowlist host + parse URL + chặn IP nội bộ |
| 9 | CRITICAL | JWT bypass | dòng 88-90 | `jwt.decode(..., verify_signature=False)` bỏ qua kiểm chữ ký; attacker tự ký token giả với `username=admin` là đăng nhập được. [A07] | `jwt.decode(token, key=JWT_SECRET, algorithms=["HS256"], options={"verify_signature": True})` |
| 10 | MEDIUM | Security Logging | dòng 97-102 | Log đang ghi thẳng `password` vào log file. Ai đọc log là lấy được credential; payload chỉ cần login bình thường là secret bị lưu lại. [A09] | `logging.info(f"Login attempt: user={username}")` |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Mã này là demo cố ý chứa lỗi để test, nhưng nếu đây là code thật thì project đang exposed rất nặng: có SQL injection, command injection, SSRF, auth bypass và lộ secret. Mình khuyên chặn release ngay cho tới khi các điểm CRITICAL/HIGH được xử lý.
