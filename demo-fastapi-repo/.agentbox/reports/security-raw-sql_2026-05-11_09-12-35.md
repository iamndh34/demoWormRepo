# security-raw-sql — saved by agentbox --save

Generated: 2026-05-11T02:12:47Z

**⚠ 4 issues across 3 files — your project is exposed to SQL injection via raw SQL interpolation**

### `auth.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | CRITICAL | SQL Injection | line 28-29 | `username` and `password` are interpolated directly into the SQL string, so attacker-controlled input can change the query syntax and bypass authentication. If `username = ' OR 1=1 --` or similar payloads are supplied, the query can be altered to return arbitrary rows. [A03] |

### `main.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | HIGH | SQL Injection | line 36 | `u_id` is inserted into an f-string inside `cursor.execute(...)`, so the SQL text changes based on the value. If that value is influenced by a request path or query parameter, an attacker can inject SQL instead of just filtering by ID. [A03] |

### `vulnerable_demo.py` (2 issues)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | CRITICAL | SQL Injection | line 28-29 | `username` is concatenated into the query string before execution, which exposes the project to SQL injection. An attacker can supply crafted input such as `x' OR '1'='1` to alter the WHERE clause and read unintended records. [A03] |
| 2 | HIGH | SQL Injection | line 67 | `account_id` is embedded directly into the SQL statement, so the query structure depends on external input. Even though it looks numeric, untrusted values can still break out of the intended predicate and change what rows are returned. [A03] |

### Summary notes

| Item | Content |
|------|---------|
| Files affected | auth.py, main.py, vulnerable_demo.py |
| Total findings | 4 |
| Highest severity | CRITICAL |

### Conclusion

The codebase looks intentionally demo-like, but the raw SQL exposure is real and immediately exploitable in the marked places. I’d block release until the query construction is switched to parameterized execution or strict allowlists for any SQL identifiers.
