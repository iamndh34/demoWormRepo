# security-raw-sql — saved by agentbox --save

Generated: 2026-05-10T19:39:46Z

**⚠ 3 issues across 2 files — Your project is exposed to SQL injection through dynamic SQL text**

### `auth.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|-------------|-------------|
| 1 | HIGH | SQL Injection | line 28-29 | The query is built with an f-string that inserts `username` and `password` directly into SQL text, so attacker-controlled input can change the WHERE clause. For example, if `username` contains `' OR 1=1 --`, the query logic can be altered and the login check can be bypassed. [A03] |

### `vulnerable_demo.py` (2 issues)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|-------------|-------------|
| 1 | HIGH | SQL Injection | line 28-29 | This code concatenates `username` into a SQL string and executes it directly, which exposes the project to SQL injection. If `username = '\' OR 1=1 --`, the query syntax changes and the attacker can influence which rows are returned. [A03] |
| 2 | HIGH | SQL Injection | line 67 | `account_id` is interpolated into the SQL text with an f-string, so the query structure depends on runtime input instead of bound parameters. Even though the value looks numeric, an attacker who can control it may inject SQL fragments on backends or paths that do not strictly enforce typing. [A03] |

### Summary notes

| Item | Content |
|------|---------|
| Files affected | auth.py, vulnerable_demo.py |
| Total findings | 3 |
| Highest severity | HIGH |

### Conclusion

This looks intentionally demo-like, but the raw SQL issues are real and immediately risky if any of these paths are reachable. I’d block release until the dynamic SQL is replaced with parameterized queries or a strict allowlist for any identifiers.
