# security-raw-sql — saved by agentbox --save

Generated: 2026-05-10T19:11:33Z

**⚠ 3 issues across 2 files — your project is exposed to SQL injection via raw SQL interpolation**

### `auth.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | CRITICAL | SQL Injection | line 27-29 | The query is built with an f-string that interpolates `username` and `password` directly into SQL text, so attacker-controlled input can change the query syntax. If `username = '\' OR 1=1 --'`, the login check can be bypassed. [A03] |

### `main.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | HIGH | SQL Injection | line 36 | `u_id` is inserted into SQL text with an f-string instead of being bound as a parameter. Even if `u_id` currently comes from the database, this pattern is unsafe and becomes exploitable the moment the source changes or the value is tainted upstream. [A03] |

### `vulnerable_demo.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | CRITICAL | SQL Injection | line 28-29 | This query concatenates `username` directly into SQL text, exposing the app to classic SQL injection. An attacker can inject payloads like `x' OR '1'='1` to alter the WHERE clause and read unintended rows. [A03] |

### Summary notes

| Item | Content |
|------|---------|
| Files affected | auth.py, main.py, vulnerable_demo.py |
| Total findings | 3 |
| Highest severity | CRITICAL |

### Conclusion

This codebase is intentionally demo-like, but the raw SQL exposure is real and immediately exploitable in the flagged paths. I’d block release until the interpolated queries are replaced with parameterized execution or strict allowlists.
