# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-10T19:44:43Z

**⚠ 10 lỗi trên 1 file — project đang EXPOSED tới injection, auth bypass, SSRF và secret leakage**

### `vulnerable_demo.py` (10 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 23-26 | Query được dựng bằng f-string với `username` rồi đưa thẳng vào `cursor.execute(...)`. Attacker chỉ cần nhập `username = `' OR '1'='1` để đọc toàn bộ bảng `users`. [A03] |
| 2 | CRITICAL | Command Injection | dòng 32-33 | `subprocess.check_output(..., shell=True)` với `host` từ request mở đường cho RCE. Ví dụ `host = 8.8.8.8; cat /etc/passwd` sẽ làm lệnh bị chèn thêm. [A03] |
| 3 | HIGH | XSS | dòng 39-40 | HTML response ghép trực tiếp `name` vào output mà không escape. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] |
| 4 | CRITICAL | Hardcoded Secret | dòng 45-46 | `SECRET_KEY` và `API_TOKEN` đang được hardcode trong source. Nếu đây là giá trị thật, attacker có thể ký token giả hoặc dùng credential để truy cập dịch vụ ngoài. [A02] |
| 5 | HIGH | Weak Hash | dòng 48-49 | Mật khẩu bị băm bằng `md5`, một thuật toán yếu và dễ brute-force/collision. Ví dụ password ngắn có thể bị crack nhanh nếu database bị lộ. [A02] |
| 6 | HIGH | IDOR | dòng 55-58 | Endpoint nhận `account_id` từ URL rồi truy vấn trực tiếp bản ghi tương ứng, không thấy kiểm tra quyền sở hữu. Attacker đổi `account_id=2` để xem tài khoản người khác. [A01] |
| 7 | CRITICAL | Unsafe Deserialization | dòng 64-66 | `pickle.loads(body)` trên dữ liệu từ request là primitive RCE kinh điển. Chỉ cần payload pickle độc hại là có thể kích hoạt gadget chain trên server. [A08] |
| 8 | HIGH | Unsafe YAML Load | dòng 72-74 | `yaml.load(..., Loader=yaml.Loader)` cho phép deserialize object không an toàn từ input người dùng. Payload YAML crafted có thể dẫn tới object injection hoặc thực thi hành vi ngoài ý muốn. [A05] |
| 9 | CRITICAL | SSRF | dòng 80-82 | `requests.get(url)` fetch thẳng URL do user cung cấp, nên attacker có thể gọi vào `http://127.0.0.1:...` hoặc metadata service nội bộ. Đây là exposure tới mạng nội bộ từ app. [A10] |
| 10 | CRITICAL | JWT Bypass | dòng 88-89 | `jwt.decode(..., verify_signature=False)` tắt xác minh chữ ký, nên token tự ký hoặc sửa payload vẫn được chấp nhận. Ví dụ đổi `username` thành `admin` là có thể giả mạo danh tính. [A07] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py |
| Tổng finding | 10 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Đây là file demo cố ý chứa lỗi để kiểm thử, nhưng nếu xuất hiện trong code thật thì project đang exposed rất nghiêm trọng và nên chặn release ngay. Mình đánh giá mức rủi ro tổng thể là CRITICAL vì có cả SQL injection, command injection, SSRF và auth bypass.
