# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-14T09:01:58Z

**⚠ 12 lỗi trên 4 file — project đang exposed tới SQL injection, auth bypass, SSRF và secret leak**

### `auth.py` (3 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CAO | SQL Injection | dòng 28-29 | Query ghép trực tiếp `username` và `password` vào SQL bằng f-string. Attacker có thể dùng payload như `' OR '1'='1` để bypass login hoặc đọc dữ liệu người dùng. [A03] |
| 2 | CAO | Hardcoded Secret | dòng 20 | `SECRET_KEY` nằm thẳng trong source code, nên nếu repo/log/build artifact lộ ra thì attacker có thể dùng key này để giả mạo token hoặc truy cập trái phép. [A02] |
| 3 | CAO | JWT Bypass | dòng 42 | `jwt.decode(..., options={"verify_signature": False})` bỏ qua xác thực chữ ký, nên token giả mạo vẫn có thể được chấp nhận. Ví dụ attacker sửa payload thành `{"username":"admin"}` rồi gửi token tự ký/không ký. [A07] |

### `vulnerable_demo.py` (9 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CAO | SQL Injection | dòng 22-24 | `username` được nhét thẳng vào câu SQL qua f-string. Payload như `' OR '1'='1` có thể làm query trả về toàn bộ user hoặc bypass điều kiện đăng nhập. [A03] |
| 2 | CAO | Command Injection | dòng 31 | `subprocess.check_output(..., shell=True)` với `host` từ request cho phép chèn lệnh, ví dụ `8.8.8.8; cat /etc/passwd` để thực thi lệnh tùy ý. [A03] |
| 3 | CAO | XSS | dòng 38-39 | `name` được phản chiếu trực tiếp vào HTML response. Payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] |
| 4 | CAO | Hardcoded Secret | dòng 44-45 | `SECRET_KEY` và `API_TOKEN` là secret thật/giả lập nằm trong code. Nếu bị lộ, attacker có thể dùng chúng để mạo danh hoặc truy cập tài nguyên liên quan. [A02] |
| 5 | CAO | Weak Hash | dòng 47 | `hashlib.md5(...)` là hàm băm yếu cho mật khẩu; attacker có thể crack nhanh bằng rainbow table hoặc brute force. [A02] |
| 6 | CAO | IDOR | dòng 54-57 | `account_id` từ client được dùng trực tiếp để lấy bản ghi mà không thấy kiểm tra ownership/permission. Attacker chỉ cần đổi ID để đọc tài khoản người khác. [A01] |
| 7 | CAO | Insecure Deserialization | dòng 63-66 | `pickle.loads(body)` trên dữ liệu do client gửi là cực nguy hiểm; payload pickle độc hại có thể dẫn tới thực thi code khi deserialize. [A08] |
| 8 | CAO | SSRF | dòng 81-83 | `requests.get(url)` fetch URL do user kiểm soát, nên attacker có thể ép server gọi nội bộ như `http://127.0.0.1:...` hoặc metadata service. [A10] |
| 9 | CAO | JWT Bypass | dòng 90-92 | `jwt.decode(..., verify_signature=False)` cho phép token giả mạo. Attacker có thể sửa payload thành user/admin bất kỳ rồi gửi vào endpoint. [A07] |

### `main.py` (2 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CAO | SQL Injection | dòng 36-37 | `u_id` được chèn trực tiếp vào SQL bằng f-string trong vòng lặp. Nếu nguồn ID bị ảnh hưởng, attacker có thể sửa syntax query để đọc dữ liệu ngoài ý muốn. [A03] |
| 2 | CAO | SSRF | dòng 44-45 | `requests.get("https://slow-api.com/data")` là call outbound cứng, nhưng nếu URL này về sau được thay bằng input người dùng hoặc redirect chain thì sẽ mở đường cho SSRF; hiện tại đây là exposure thấp hơn nên mình giữ mức cảnh báo thấp hơn các case trực tiếp. [A10] |

### `requirements.txt` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | TRUNG BÌNH | Outdated Dependency | dòng 2 | `requests==2.20.0` là version cũ và có thể kéo theo rủi ro CVE/supply-chain. Với app có outbound request, dependency lỗi thời làm tăng exposure nếu có exploit công khai. [A06] |

### `Dockerfile` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CAO | Secret Leak | dòng 14 | `ARG GITHUB_TOKEN=ghp_secret_123456789` đưa secret vào build context/history. Nếu image hoặc layer history bị truy cập, attacker có thể lấy token và dùng nó ngoài ý muốn. [A02] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | auth.py, vulnerable_demo.py, main.py, requirements.txt, Dockerfile |
| Tổng finding | 12 |
| Mức độ cao nhất | CAO |

### Kết luận

Đây là code demo cố ý chứa lỗi, nhưng nếu áp vào code thật thì project đang exposed nghiêm trọng tới SQL injection, auth bypass, SSRF và secret leak. Mức rủi ro tổng thể đủ cao để chặn release nếu các pattern này xuất hiện ngoài môi trường test.
