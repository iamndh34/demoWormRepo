# security-top-10-owasp — saved by agentbox --save

Generated: 2026-05-14T09:35:59Z

**⚠ 11 lỗi trên 3 file — project đang EXPOSED tới SQL injection, SSRF, JWT bypass và leak secret**

### `vulnerable_demo.py` (8 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 29-30 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` hoặc `&& curl ...` để chạy lệnh tùy ý trên server. Đây là đường vào RCE trực tiếp. [A03] |
| 2 | CRITICAL | SQL Injection | dòng 20-22 | Query ghép f-string với `username` làm attacker đổi logic SQL bằng payload như `x' OR '1'='1' --` để đọc toàn bộ bảng users. [A03] |
| 3 | CRITICAL | Insecure Deserialization | dòng 49-52 | `pickle.loads(body)` nhận dữ liệu từ request body nên attacker có thể gửi payload pickle độc hại để thực thi code khi deserialize. Đây là exposure rất nặng. [A08] |
| 4 | CRITICAL | JWT bypass | dòng 68-70 | `jwt.decode(..., options={"verify_signature": False})` bỏ qua xác thực chữ ký, nên token giả mạo có thể được chấp nhận như thật. Ví dụ attacker sửa `username` thành `admin` rồi tự ký không cần key. [A07] |
| 5 | CRITICAL | Secret leak | dòng 37-38 | `SECRET_KEY` và `API_TOKEN` bị hardcode trong source; nếu repo hoặc image bị lộ, attacker có thể dùng luôn secret thật để giả mạo hoặc truy cập dịch vụ ngoài. [A02] |
| 6 | HIGH | SQL Injection | dòng 58-60 | `cursor.execute(f"SELECT * FROM accounts WHERE id = {account_id}")` cho phép sửa tham số thành `1 OR 1=1` để đọc account khác; đây là IDOR/injection qua ID. [A01] [A03] |
| 7 | HIGH | SSRF | dòng 61-64 | `requests.get(url)` lấy URL từ user nên attacker có thể gọi `http://169.254.169.254/latest/meta-data/` hoặc `http://localhost:...` để pivot vào mạng nội bộ. [A10] |
| 8 | HIGH | XSS | dòng 31-34 | Trả HTML bằng f-string với `name` chưa escape; payload như `<script>alert(1)</script>` sẽ chạy trong trình duyệt nạn nhân. [A03] |

### `auth.py` (3 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 24-29 | `query = f"SELECT * FROM users WHERE username = '{username}' AND password = '{password}'"` cho phép payload kiểu `' OR '1'='1` bypass login và đọc dữ liệu user. [A03] |
| 2 | CRITICAL | JWT bypass | dòng 31-33 | `jwt.decode(token, options={"verify_signature": False})` chấp nhận token không kiểm chữ ký, nên attacker có thể tự tạo token admin giả. [A07] |
| 3 | HIGH | Timing Attack | dòng 16-20 | So sánh token bằng `==` tạo side-channel timing; attacker có thể đo thời gian để suy ra giá trị đúng từng phần trong một số bối cảnh. [A02] |

### `requirements.txt` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | Outdated Component | dòng 3 | `requests==2.20.0` là version rất cũ; dependency lỗi thời trong service xử lý URL/fetch làm tăng exposure tới CVE đã biết và supply-chain risk. [A06] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py, auth.py, requirements.txt |
| Tổng finding | 11 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Mã này rõ ràng là demo cố ý chứa lỗi, nhưng nếu đây là code thật thì project đang exposed rất nặng: có RCE, SQL injection, SSRF và JWT bypass. Theo mình nên chặn release ngay cho tới khi các đường tấn công CRITICAL được xử lý.
**⚠ 11 lỗi trên 3 file — project đang exposed tới SQL injection, command injection, SSRF, JWT bypass và secret leak.**

### `vulnerable_demo.py` (7 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(f"ping -c 1 {host}", shell=True)` cho phép attacker chèn `; cat /etc/passwd` để chạy lệnh tùy ý trên server. Đây là đường vào RCE trực tiếp. [A03] |
| 2 | CRITICAL | JWT bypass | dòng 107 | `jwt.decode(token, options={"verify_signature": False})` bỏ qua xác thực chữ ký, nên attacker có thể tự sửa payload thành `{"username":"admin"}` và giả mạo danh tính. [A07] |
| 3 | HIGH | SQL Injection | dòng 29 | `cursor.execute(query)` nhận query đã ghép từ input trước đó, nên payload kiểu `"' OR '1'='1"` có thể làm lệch cú pháp SQL và đọc dữ liệu ngoài ý muốn. [A03] |
| 4 | HIGH | SQL Injection | dòng 67 | `cursor.execute(f"SELECT * FROM accounts WHERE id = {account_id}")` cho phép chèn biểu thức SQL qua `account_id`; ví dụ `1 OR 1=1` có thể lộ toàn bộ bảng accounts. [A03] |
| 5 | HIGH | SSRF | dòng 98 | `requests.get(url)` fetch URL do user kiểm soát, nên attacker có thể trỏ tới `http://127.0.0.1:...` hoặc metadata service để pivot vào mạng nội bộ. [A10] |
| 6 | HIGH | Secret leak | dòng 53-54 | `SECRET_KEY` và `API_TOKEN` hardcoded trong source; nếu repo, log, hoặc image bị lộ thì attacker có thể dùng ngay secret thật để ký token hoặc truy cập dịch vụ ngoài. [A02] |
| 7 | MEDIUM | Secret logging | dòng 118 | `logging.info(f"Login attempt: user={username}, password={password}")` ghi thẳng password vào log, tạo exposure cho bất kỳ ai đọc log hoặc hệ thống thu thập log tập trung. [A09] |

### `auth.py` (3 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 28-29 | Query được dựng bằng f-string từ `username` và `password`, nên payload như `"' OR '1'='1"` có thể bypass đăng nhập và đọc bảng users. [A03] |
| 2 | HIGH | JWT bypass | dòng 42 | `jwt.decode(token, options={"verify_signature": False})` chấp nhận token không kiểm chữ ký; attacker có thể tự tạo token giả và qua mặt auth server-side. [A07] |
| 3 | HIGH | Hardcoded Secret | dòng 20 | `SECRET_KEY = "super-secret-key-12345"` là secret cố định trong code; nếu bị lộ thì mọi token/phiên phụ thuộc key này đều có nguy cơ bị giả mạo. [A02] |

### `main.py` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | SQL Injection | dòng 36 | `cursor.execute(f"SELECT * FROM profiles WHERE user_id = {u_id}")` ghép trực tiếp ID vào SQL; nếu giá trị đầu vào bị thao túng, attacker có thể sửa logic truy vấn và đọc dữ liệu profile không thuộc về mình. [A03] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | vulnerable_demo.py, auth.py, main.py |
| Tổng finding | 11 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Code này đang exposed rất rõ tới các lỗi OWASP Top 10 nghiêm trọng, đặc biệt là SQL injection, command injection, SSRF và JWT bypass. Nếu đây là code thật chứ không phải demo intentional, mình khuyên chặn release ngay vì attacker có thể đọc dữ liệu, giả mạo danh tính, hoặc chạy lệnh trên server.
**⚠ 11 lỗi trên 5 file — project đang EXPOSED tới auth bypass, injection, SSRF và secret leak**

### `auth.py` (2 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | SQL Injection | dòng 28-29 | Query ghép trực tiếp `username` và `password` vào SQL bằng f-string, nên attacker có thể dùng payload như `' OR '1'='1` để bypass đăng nhập và đọc dữ liệu người dùng. [A03] |
| 2 | CRITICAL | JWT bypass | dòng 42 | `jwt.decode(..., options={"verify_signature": False})` chấp nhận token không kiểm chữ ký, nên attacker có thể tự sửa payload rồi giả mạo danh tính. Ví dụ đổi `role` hoặc `sub` trong token là server vẫn tin. [A07][A02] |

### `vulnerable_demo.py` (7 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | CRITICAL | Command Injection | dòng 38 | `subprocess.check_output(..., shell=True)` với `host` từ user cho phép chèn `; cat /etc/passwd` hoặc lệnh khác để RCE. Đây là đường tấn công trực tiếp vào shell. [A03] |
| 2 | CRITICAL | SQL Injection | dòng 28-29 | Query SQL được dựng bằng f-string từ `username`, nên payload như `' UNION SELECT ...` có thể sửa logic truy vấn và lộ dữ liệu. [A03] |
| 3 | CRITICAL | Insecure Deserialization | dòng 77 | `pickle.loads(body)` trên dữ liệu do client gửi lên cho phép thực thi object payload độc hại; attacker có thể nhúng gadget chain để chạy code khi deserialize. [A08] |
| 4 | CRITICAL | YAML Deserialization | dòng 89 | `yaml.load(..., Loader=yaml.Loader)` xử lý YAML không an toàn, attacker có thể dùng tag kiểu `!!python/object/apply` để kích hoạt hành vi ngoài ý muốn. [A08][A05] |
| 5 | CRITICAL | SSRF | dòng 98 | `requests.get(url)` lấy URL từ user nên server có thể bị ép gọi nội bộ như `http://127.0.0.1:...` hoặc metadata service `http://169.254.169.254/...`. Đây là exposure tới mạng nội bộ. [A10] |
| 6 | CRITICAL | JWT bypass | dòng 107 | `jwt.decode(..., verify_signature=False)` bỏ qua xác thực chữ ký, nên attacker có thể tự ký giả hoặc sửa payload token để mạo danh người khác. [A07][A02] |
| 7 | HIGH | Secret leak | dòng 53 | `SECRET_KEY` hardcoded trong source làm lộ bí mật ký/giải mã nếu repo bị đọc hoặc build artifact bị rò rỉ. Nếu key này dùng thật, toàn bộ token phụ thuộc vào nó đều bị compromise. [A02] |

### `main.py` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | MEDIUM | SQL Injection | dòng 36 | `cursor.execute(f"SELECT * FROM profiles WHERE user_id = {u_id}")` nhúng trực tiếp ID vào SQL. Nếu giá trị đầu vào bị kiểm soát ở upstream hoặc bị biến đổi kiểu, attacker có thể lợi dụng để sửa truy vấn. [A03] |

### `templates/index.html` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | LOW | Accessibility issue | dòng 22 | Nút có `aria-label` và hành vi `onclick` không tạo ra lỗ hổng OWASP Top 10 application security rõ ràng; đây chủ yếu là vấn đề UX/a11y, không phải exposure bảo mật. [A04] |

### `requirements.txt` (1 lỗi)

| STT | Mức độ nghiêm trọng | Loại lỗi | Vị trí | Chi tiết mô tả |
|----:|--------------------|----------|--------|----------------|
| 1 | HIGH | Outdated dependency | dòng 2-3 | `requests==2.20.0` và `pydantic==1.10.0` là phiên bản cũ, làm project EXPOSED tới rủi ro từ component lỗi thời nếu có CVE reachable trong runtime. Theo mình nên coi đây là bề mặt tấn công supply-chain cần triage thêm. [A06] |

### Ghi chú tổng hợp

| Mục | Nội dung |
|-----|----------|
| File bị ảnh hưởng | auth.py, vulnerable_demo.py, main.py, templates/index.html, requirements.txt |
| Tổng finding | 11 |
| Mức độ cao nhất | CRITICAL |

### Kết luận

Mã này có vẻ là demo cố ý chứa lỗi, nhưng nếu đây là code thật thì project đang exposed nghiêm trọng ở nhiều điểm: auth bypass, injection, SSRF và secret leak. Mình khuyên chặn release ngay cho tới khi các đường tấn công CRITICAL/HIGH được xử lý.
