# security-raw-sql — saved by agentbox --save

Generated: 2026-05-11T02:13:25Z

**⚠ 4 issues across 3 files — Your project is exposed to SQL injection through dynamic SQL text**

### `auth.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | CRITICAL | SQL Injection | line 28-29 | The query is built with an f-string that inserts `username` and `password` directly into SQL text, so attacker-controlled input can change the query syntax and bypass authentication. Example: if `username = "' OR 1=1 --"`, the WHERE clause can be altered. [A03] |

### `main.py` (1 issue)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | HIGH | SQL Injection | line 36 | `u_id` is interpolated into the SQL string with an f-string, so a crafted value can change the query structure instead of being treated as data. If this value ever becomes attacker-controlled, the endpoint is exposed to SQL injection. [A03] |

### `vulnerable_demo.py` (2 issues)

| # | Severity | Issue Type | Location | Description |
|--:|----------|------------|----------|-------------|
| 1 | CRITICAL | SQL Injection | line 26-29 | The query concatenates untrusted `username` into SQL text, which lets attacker input alter the query syntax and read arbitrary rows. A payload like `"' OR '1'='1"` can turn the filter into a tautology. [A03] |
| 2 | HIGH | SQL Injection | line 67 | `account_id` is inserted directly into the SQL statement, so any path where this value is influenced can become injectable. Even numeric-looking inputs are unsafe when they are spliced into SQL text instead of bound as parameters. [A03] |

### Summary notes

| Item | Content |
|------|---------|
| Files affected | auth.py, main.py, vulnerable_demo.py |
| Total findings | 4 |
| Highest severity | CRITICAL |

### Conclusion

This codebase looks intentionally demo-like, but the raw SQL exposure is real and directly exploitable in the highlighted paths. I’d block release until the f-string SQL is replaced with parameterized queries or a strict allowlist for identifiers.
